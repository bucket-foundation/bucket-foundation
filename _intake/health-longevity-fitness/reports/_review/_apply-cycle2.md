CYCLE 2 APPLY — line-edits from the prose-editor read. Apply your slice's findings to the
chapter markdown. Work dir: /home/gian/agfarms/bucket-foundation/_intake/health-longevity-fitness/reports

You get: your slice's findings file (_review/c2-N.md) listing located edits per chapter, and the
chapter files themselves. Apply the findings, using judgment.

NOTE: the word "genuinely" has ALREADY been removed globally — skip those findings.

APPLY these finding types:
- **AITELL**: swap "robust/robustly" → "replicated"/"consistent"/"strong" (per context); remove
  "boost", "really"/"truly" as filler intensifiers; kill any "it's worth noting"/"that said".
- **DEADWEIGHT / FLAB**: tighten wordy phrases, throat-clearing, nominalizations, redundant hedges
  — apply the finding's suggested tighter version.
- **RHYTHM**: split the flagged run-on sentences / mega-bullets; vary where the finding says.
- **TRANSITION**: add the flagged bridging sentence where momentum drops.
- **ANTITHESIS**: rewrite ONLY the incidental comma-splice antitheses the finding explicitly marks
  for rewrite (recast as a plain positive statement or two clean sentences).
- **Specific bugs**: dedup internal self-repeats (e.g. a thesis stated twice, a cross-ref repeated
  3x); fix the ch.09 garbled "not smoking…" list so it doesn't read as a negation; fix any malformed
  `**bold**` nesting (ch.23).

PROTECT (do NOT touch):
- Every sentence the findings flag as **GOOD**.
- The load-bearing motifs and marquee hooks, even when they look like antithesis: "predictor ≠ lever",
  "mechanism ≠ outcome", "regulate, not boost", "healthspan, not lifespan", "being alive is a verb,
  not a noun", "controlled, not cured", and each chapter's opening hook. When unsure, KEEP.
- Defined grade words ("REAL"/"robust" where it's a defined tier/term, e.g. Fried frailty "0 = robust").

HARD RULES: never change a number, effect size, claim, evidence tier, citation, cross-ref, a
`@@FIG:...@@` marker, or a `[^footnote]`. Preserve meaning exactly. Don't flatten the voice.

Edit the chapter markdown in place. Reply with: # edits applied, by type, and anything you deliberately left.
