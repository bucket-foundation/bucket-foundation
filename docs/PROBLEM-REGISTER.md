# Problem Register

Living list. One row per problem. Status: open, in_progress, fixed, wontfix. Bead column fills once the Nucleus instance is back and `BEADS-PENDING.jsonl` drains. Add rows at the bottom, never renumber. Target design lives in `docs/ARCHITECTURE.md`.

| ID | Area | Sev | Problem | Evidence | Solution | Bead | Status |
|---|---|---|---|---|---|---|---|
| PR-001 | data | P0 | Five `public` tables take anon-key browser writes, no migration, no RLS | `src/context/AuthorContext.tsx:40-47`, `CiteTokensContext.tsx:58`, `src/lib/story/index.ts:179-229` | Deny-by-default RLS now; move writes to service-role routes; retire tables into `app.*` | pending | open |
| PR-002 | security | P0 | research-tools gateway public, unauthenticated, unthrottled | `services/research-tools/gateway.py`, no `limit_req` in nginx conf | Shared-secret header checked in gateway + nginx `limit_req` | pending | open |
| PR-003 | security | P0 | 54 API routes CORS `*`, no auth, no throttle; `canon/search` claims a limit it lacks | `src/app/api/research/hhfit/route.ts:33`, `canon/search/route.ts:11` | Per-IP token bucket in shared lib; CORS `*` only on feed402 surfaces | pending | open |
| PR-004 | identity | P0 | Wallet, email, handle are three disconnected rows per person | `author` (Dynamic), `auth.users` (NextAuth), `bucket.academy_profiles` | `app.identities` keyed on `auth.users.id`, unique wallet + handle, backfill | pending | open |
| PR-005 | cost | P1 | x402 daily cap is a per-instance module variable | `src/app/api/research/route.ts:92` | `app.spend_ledger` atomic upsert | pending | open |
| PR-006 | cost | P1 | Viatika meter stub always ok; `/api/chat` no per-user limit | `src/lib/meter.ts:29-33`, `api/chat/route.ts:143-154` | Wire real meter; per-user quota from `spend_ledger` | pending | open |
| PR-007 | cost | P1 | hte campaigns have no dollar ceiling | `tools/hypothesis-engine/hte/runner.py` | Per-campaign cap in runner; log to `spend_ledger` | pending | open |
| PR-008 | reliability | P1 | Tutor LLM + mirror timers run on founder laptop as production | `scripts/llm/systemd/*.service`, `scripts/systemd/*` | Move to Hetzner or degrade gracefully when tunnel down | pending | open |
| PR-009 | data | P1 | `academy_profiles` DDL outside migrations; env var drift `SERVICE_ROLE_KEY` vs `SERVICE_KEY` | `scripts/sql/academy_profiles.sql`, `hte/corpus/production.py:748` | Move into `supabase/migrations/`; one env name | pending | open |
| PR-010 | data | P1 | Branch holds 2 of 18 migrations; `graph` schema code ships without its DDL | `origin/main` has 16 `graph` migrations | Pull migrations; CI drift check | pending | fixed on dev (18 migrations at 0b57b2ebc; the review base lacked them); drift check open |
| PR-011 | security | P2 | photon-api hardcoded `bucket/bucket` credential fallback | `services/photon-api/server_pg.py:18-22` | Fail fast when env missing | pending | open |
| PR-012 | engineering | P1 | 40 research proxies duplicate 135-220 line scaffold, 5,815 lines | `src/app/api/research/*/route.ts` | `[tool]/route.ts` + `TOOL_REGISTRY` + `src/lib/research-gateway.ts` | pending | open |
| PR-013 | engineering | P1 | No CI gate; 863 hte tests local only; ESLint off in builds | `.github/workflows/`, `next.config.mjs:14` | lint + tsc + build + pytest on PR | pending | open |
| PR-014 | repo | P1 | 2.3 GB `.git`, 10 corpora dirs tracked | `yt` 2.2G, `archive` 3.6G, `openalex` 1G, `quantum` 802M, `pubmed` 234M | gdrive mirror, gitignore, coordinated `git filter-repo` | pending | open |
| PR-015 | engineering | P2 | `next-auth@4` + `@auth/core@0.37` both installed | `package.json` | Finish v5 migration before new auth surfaces | pending | open |
| PR-016 | product | P2 | ~45 of ~75 routes off the core loop; 4 duplicate clusters | about/mission/manifesto, contribute/join, contributors/m, academy/learn/library/knowledge | Merge clusters; `apps/labs` for tools, chat, sacred-history, ladder | pending | open |
| PR-017 | product | P1 | No author earnings, no rate card, no agent receipts; payment page is a v0.1 prototype | `/support`, `/cite-forever/v0.1` | `app.citations` + `app.payouts` + `app.agent_keys` + three pages | pending | open |
| PR-018 | data | P3 | Citation receipts on Arweave + EAS with no queryable index | `src/lib/permanence/dual-write.ts` | `app.citations` row per receipt | pending | open |
| PR-019 | engineering | P2 | hte Research OS API built and unwired to MCP; `hte-serve` not under systemd | `hte/mcp_tool.py`, `mcp-server/bucket-mcp.py` | Wire `TOOL_DEFINITION`; add unit | pending | fixed 2026-09-18: the hosted MCP endpoint carries `hypothesize` (#176); `scripts/systemd/hte-serve.service` runs the engine on 127.0.0.1:8420 (#184) |
| PR-020 | repo | P2 | 22 root markdown files, two roadmaps, `tools/shorts` dead stub | repo root, `BEAD_BACKLOG.md`, `ROADMAP.md` | `docs/` fold, one roadmap, delete stub | pending | open |
| PR-021 | security | P0 | Story signing key read from `NEXT_PUBLIC_WALLET_PRIVATE_KEY` in a module imported by a `"use client"` component; key is bundled to every browser if set, and `console.log`ged | `src/lib/story/index.ts:17,19,55,99,208`, `src/app/research/PublishForm.tsx:5` | Rename to server-only `STORY_WALLET_PRIVATE_KEY`; move mint to an API route; delete the log; rotate any key that was ever set | pending | open |
| PR-022 | payments | P0 | No real x402 payment exists: server signer returns `null`, client signer emits `"unsigned"`/`"stub-signed"`; no USDC moves, no receipt persists | `src/lib/x402-pay.ts:45-77`, `src/lib/feed402-client.ts:82-86`, `api/research/route.ts:125-139` | Implement EIP-3009 signing server-side with a funded Base wallet; persist receipt to `app.citations`; return real `receipt.status` | pending | open |
| PR-023 | architecture | P1 | Two disconnected citation economies: legacy Story Iliad testnet + Walrus + `public` tables (author publish), and feed402 envelope + stub x402 (reader cite); no shared code or data | `src/lib/story/index.ts`, `src/app/research/PublishForm.tsx`, `/api/research` | Pick feed402 + x402 on Base as the one economy; Story mint becomes an optional post-payment step; retire Walrus/Iliad path or move to `apps/labs` | pending | open |
| PR-024 | content | P2 | Branch count disagrees across four sources: layout says 8, `canon.ts` has 11, `canon-fs.ts` has 10 with duplicate `num: "09"`, CLAUDE.md says 7 | `src/app/layout.tsx:136,177,202`, `src/lib/canon.ts:110-215`, `src/lib/canon-fs.ts:46-55` | One `branches.json` under `src/data/`, every consumer imports it; fix the `09` collision | pending | open |
| PR-025 | product | P2 | `/canon/search` renders the same component as `/canon`; `/canon/timeline` is a redirect stub; `/join` duplicates `/contribute` with a dead "Mint it" CTA | `src/app/canon/search`, `canon/timeline/page.tsx`, `src/app/join/page.tsx` | Keep `/canon` and `/contribute`; 301 the others | pending | open |
| PR-026 | product | P2 | `/research/agent` is dark unless the founder laptop GPU tunnel is up; one-line offline copy | `src/app/research/agent`, `ResearchAgentClient` | Tie to PR-008; show explicit status banner and disable input when offline | pending | open |
| PR-027 | seo | P3 | Sitemap omits dynamic routes (`/canon/claims/*/*`, `/canon/bridges/[slug]`, `/contributors/[handle]`, `/research/education/*`) and lists off-loop pages; figure pages lack OG metadata | `src/app/sitemap.ts`, `canon/[slug]/figures/[figure]` | Generate sitemap from the same data libs; add `generateMetadata` to figure pages | pending | open |
| PR-028 | content | P3 | In-page TODOs shipped to production: GitHub Sponsors, CSV/Zenodo mirrors, wallet lookup | `/support`, `/research/datasets/[slug]`, `contributors/[handle]/page.tsx:62` | Close each or remove the promise from the page | pending | open |
| PR-029 | hygiene | P3 | Zero-byte unused `IpMetadataContext.tsx` | `src/context/IpMetadataContext.tsx` | Delete | pending | open |
| PR-030 | data | P2 | One Supabase instance hosts two unrelated tenants (`public` legacy stack, `bucket` academy) under the same env vars | `NEXT_PUBLIC_SUPABASE_URL` shared | Fold both into the `app` schema per `docs/ARCHITECTURE.md`; one identity table | pending | open |
| PR-031 | repo | P1 | `archaeology/` holds 84,152 tracked files, 1.5 GB; git object count dominates clone and diff time | `git ls-files archaeology \| wc -l` | gdrive mirror + manifest in repo; include in the PR-014 history rewrite | pending | open |
| PR-032 | repo | P1 | Branch policy (`main`/`dev`/`hte/integration`) exists only on unmerged branch `docs/hte-branch-policy`; current branch `run/quantum-history-001` branches off `main`, 8 ahead / 146 behind, and violates it | commit `d8dde856`, `git merge-base` | Merge the policy to `main` first; rebase `run/*`, `hte/*`, `fix/hte-*` onto `hte/integration` | pending | fixed 2026-09-18: `run/quantum-history-001` landed on `dev` as #4 and #10, and the main checkout moved to `dev` |
| PR-033 | repo | P2 | Dead media in git: `gtm/` 1.6G, `manifesto-source/` 76M, `figma-export/` 14M (gdrive is canonical per org policy), `museum/`, `checkpoints/`; `blog/` 74M of mirrored third-party HTML | dir sizes, 0 code refs | gdrive mirror, one-line pointer README each, fold into PR-014 rewrite | pending | open |
| PR-034 | repo | P2 | 19 stale `gone` branches; 13 worktrees (~49 GB on disk) tracking unmerged branches touched today | `git branch -vv`, `git worktree list` | Land or abandon each PR, then `git branch -D` + `git worktree remove` | pending | fixed 2026-09-18: uncommitted work squashed onto `dev`, worktrees removed, merged branches deleted |
| PR-035 | repo | P2 | Five `package.json` scripts point at missing Story Protocol scripts | `mint-and-register`, `*-spg`, `register-deriv-*`, `create-spg-collection` | Delete the entries or restore the files under `apps/labs` | pending | open |
| PR-036 | packaging | P2 | Four `requirements*.txt` with conflicting numpy pins; no Python lockfile; four Dockerfiles with no shared base | `services/*/requirements.txt`, `tools/hypothesis-engine/pyproject.toml` | One constraints file or `uv lock` per service; shared base image | pending | open |
| PR-037 | repo | P2 | `grants-gateway/` (256M) and `polingual/` (453M) are standalone apps with their own `package.json`, lockfile, `node_modules`, Dockerfile inside this repo | dir READMEs, own `deploy.sh` | Extract to own repos; pointer README | pending | open |
| PR-038 | tests | P2 | Five `scripts/test-*.ts` and six `learning/app/test-*.mjs` are orphaned; `npm test` runs one token script | `package.json` `test` | Wire into one `npm test` runner and CI (PR-013) | pending | open |
| PR-039 | privacy | P2 | `grants-targets/prep/` drafts hold contact PII for external orgs; `private/issuer-key.json` on disk | `grants-targets/prep/*`, `private/` | Move PII drafts to `private/`; confirm issuer key exists nowhere tracked; document rotation | pending | open |
| PR-040 | repo | P3 | `local/patents` duplicates `data/patents`; `.beads/` is 156M | `local/`, `.beads/backup` | Fold into `data/`; prune bead backups | pending | open |
| PR-041 | docs | P1 | Org and venture CLAUDE.md both say canon lives on gdrive; the live site reads git-tracked `bucket-canon/` at request time since 2026-05-15 | `CLAUDE.md:49`, `src/lib/canon.ts:14` | Repo is canonical; gdrive becomes a generated read-only export; fix both docs | pending | open |
| PR-042 | reliability | P1 | Canon Lambda depends on an exact `outputFileTracingIncludes` glob; a new canon subtree that misses the glob ships an empty 503 Lambda with no build check; prior 413 MB incident | `next.config.mjs:17-33` | Build-time assertion that every dir `canon-fs.ts` reads is traced; long term, serve canon from Postgres pointers + static export | pending | open |
| PR-043 | security | P2 | `NEXTAUTH_SECRET` falls back to the literal `"dev-unconfigured-secret"`; hardcoded Story testnet contract and RPC defaults | `src/lib/auth.ts:52`, `src/lib/story/index.ts:13` | Throw on missing secret in production; no chain defaults | pending | open |
| PR-044 | data | P2 | Laptop-only stores with no mirror: `.fts.sqlite` 540 MB (24,196 docs), `grants.db` 129 MB, pgvector `polingual` 6.5M rows | root, `grants-gateway/data/`, `services/photon-api` | Nightly `pg_dump` + rclone to gdrive; document as research backends, never deploy targets | pending | open |
| PR-045 | data | P2 | Photon corpus in three copies at different completeness (pgvector 6.5M, `all.json` 45K in prod, 448 MB `index.sqlite`) | `services/photon-api/server_pg.py`, `src/lib/photon-index.ts:11` | `all.json` is the only prod input; state it in code and README | pending | open |
| PR-046 | data | P3 | `PINATA_JWT` declared, read by nothing; `WALRUS_*` used only by the legacy path | `.env.example` | Prune env template with each retired path | pending | open |
| PR-047 | data | P2 | K3s Postgres manifest exists with no code confirming it runs; USPTO loader points at it | `deploy/k8s/postgres.yaml`, `data/patents/uspto/scripts/load_patentsview.py:112` | Decide: delete the manifest or make it the home for patents data with a backup job | pending | open |
| PR-048 | reliability | P1 | `/library` and `/knowledge` throw and 500 in production when `NEXT_PUBLIC_DYNAMIC_ENV_ID` is unset; only `/research` got the graceful-degrade fix | `src/providers/Web3Providers.tsx:67-68`, `ResearchPublishClient.tsx:7-9` | Retire both pages with the legacy Web3 path (PR-023); until then, guard with the same fallback | pending | open |
| PR-049 | product | P2 | `/library` has Open/Cite buttons with no handler and non-standard `<text>` tags; duplicates `/research/papers` | `src/app/library/page.tsx:24-33` | Delete with PR-048 | pending | open |
| PR-050 | cost | P1 | `/api/chat` pre-charges a flat $0.02 with no true-up; tutor and chat are the largest Anthropic spend surfaces and both sit behind the always-ok meter stub | `src/app/api/chat/route.ts:23-25`, `api/academy/tutor/route.ts:140` | Keep `NEXT_PUBLIC_CHAT_ENABLED` off until Viatika meter is wired; token-cost true-up from the response usage block | pending | open |
| PR-051 | product | P2 | "Publish to canon" is a `setTimeout` stub shipped on all 40 tool pages; waitlist on `/chat` is a `mailto:` | `_shared/runner.tsx` `PublishToCanon`, `src/app/chat/page.tsx:44` | Remove the button until a real intake route exists; waitlist row in `app.audit_log` or a form endpoint | pending | open |
| PR-052 | engineering | P2 | Mastery formula implemented twice (`adaptive.js` client, `mastery.ts` server) with no parity test | `learning/app/js/adaptive.js`, `src/lib/academy/mastery.ts` | Shared fixture file both suites assert against; long term one TS module compiled for both | pending | open |
| PR-053 | engineering | P3 | `labbrain` hand-rolls the submit/poll lifecycle instead of `_shared/runner.tsx`; `/research/tools` copy says forty, catalog is 40 | `LabBrainClient.tsx` | Port to `useToolRun` during PR-012 | pending | open |
| PR-054 | product | P2 | Tool runs persist nowhere; a refresh loses the result; no analytics to know whether any tool has a user | `src/app/research/tools/*` | Vercel analytics events per submit; optional `app.tool_runs` row keyed by agent key once PR-017 lands | pending | open |
| PR-055 | product | P3 | `/learn` "corpus.zip" contains 2 text files | `src/app/learn/LearnTabs.tsx` | Generate from `bucket-canon/` at build or rename the button | pending | open |
| PR-056 | identity | P0 | Three sign-in stacks (Supabase OTP, NextAuth v4 at `api/auth/[...nextauth]`, Dynamic web3) and seven pages with their own OTP form and component-held token; no site-wide session, no protected-route middleware, no account page, no site-wide sign-out | `src/app/research-os/{workspace,class,review,roster,edges,profile}/page.tsx`, `src/app/canon/signoff/page.tsx`, `src/lib/auth.ts`, `src/providers/Web3Providers.tsx` | Supabase cookie sessions via `@supabase/ssr` in middleware, server components, and route handlers; one `/sign-in`; one account page; NextAuth retired; Dynamic as a linked wallet on `app.identities`; forms removed | pending | in_progress |
| PR-057 | architecture | P1 | Research OS (16 migrations on dev, 20 API routes, 7 pages, 494 tests, the engine seam) is absent from the as-is and from `ARCHITECTURE.md`'s product model, which describes the citation loop as the product; the founder's decision of 2026-09-15 (`learning/research-os/INTEGRATION-PLAN.md`) makes Research OS the product with the loop as its backbone | `docs/ARCHITECTURE-REVIEW-2026-09-14.md` "System as-is", `docs/ARCHITECTURE.md` "Product" | Product section rewritten (this commit); `apps/labs` holds only what INTEGRATION-PLAN.md section 9 freezes | pending | fixed |
| PR-058 | identity | P1 | The Academy frame keeps its own Supabase session (`storageKey: "bucket-academy/auth"`), so a person signed in on the site is signed out inside the frame | `learning/app/js/auth.js:159-168` | Bridge the site session into the frame (postMessage `setSession`) or move the frame to the shared cookie client | pending | open |
| PR-059 | graph | P1 | The graph's 41 primes are the first Academy atom of each course, no factor edge crosses a branch, and nothing stands for equality, number, or set: the bottom layer is where each course starts | `learning/research-os/PRIMES.md` "Results on the local graph" | The decompose-further queue (`scripts/research-os/decompose-further.ts`) proposes factors across branches and missing base ideas, checked by a second model and Wikipedia links, decided at `/research-os/edges` | ros-prime 2 | in_progress |
| PR-060 | graph | P2 | The 105 canon concept tags mix people (33, such as Euler and Tesla) with ideas, so decomposition leaves all of them out | `src/lib/research-os/decompose-further.ts` `IDEA_SOURCES` | Split each tag into a link to its figure node or a concept node the queue can decompose | ros-prime 5 | open |
| PR-061 | graph | P2 | One concept can sit in two branches as two nodes (cosmology's and physics' "Equivalence principle"); the decompose-further verifier refused a factor pair between them as a duplicate | `learning/research-os/PRIMES.md` Results | Find same-concept pairs by title and embedding, queue them for review, merge by superseding one node and moving its edges | ros-graph-dedup | open |
| PR-062 | security | P1 | `verifyReviewer` accepts any class teacher or librarian, and anyone signed in can create a class and teach it, so routes behind it that change shared state beyond a class open to any account: canon sign-off, the frontier flags, and production approval, which creates a public node and a confidence-1 `derives_from` edge, with no check against approving one's own production | `src/lib/research-os/reviewer.ts` `holdsStaffRole`, `src/lib/research-os/classes.ts:76`, `src/app/api/canon/signoff/route.ts`, `src/app/api/research-os/frontier/route.ts`, `src/app/api/research-os/review/route.ts` | Move those routes to `verifyGraphReviewer` (allowlist) or a role granted by an admin, and refuse self-review; graph review moved in PR #186 | pending | open |
| PR-063 | graph | P2 | A missing idea whose name bundles two existing ideas ("Vector spaces, bases and inner products" beside "Vector spaces" and "Inner product spaces") comes through as new, since consolidation matches a whole name to one node and approval reuses a node by slug alone; approving it adds a duplicate | `learning/research-os/PRIMES.md` Results | Split a bundled name on "and" and commas, match each part to a node, and offer the matches as factor proposals in place of a new node | ros-graph-dedup | open |
| PR-064 | engineering | P1 | The Vercel gate's diff check never ran on Vercel: the build clone holds the pushed commit alone, so `git diff <previous sha> <sha>` failed and every push without `[skip ci]` built, bead-only commits included; `echo \| grep -q` under `pipefail` would also have skipped a site change in a diff past 64 KB | Build logs of `dpl_AuJ345hdvDXV9D4f2LQKyAUvcwSy` and `dpl_6w7kyAVDibVDpU2b3icFtAQxj8C3`; `docs/VERCEL-BUILDS.md`, Measured on 2026-09-18 | Fetch the base's trees at depth 1 from the public repository, match the allowlist from a here-string, build on any failure | ros-builds | fixed on PR #187 |
| PR-065 | engineering | P0 | Vercel builds on Node 20 fail from 2026-10-01, per every build log since 2026-09-18 | Build log warning "Node.js version 20.x is deprecated. Deployments created on or after 2026-10-01 will fail to build" | `engines.node` 24.x in `package.json`, CI on Node 24; `next build` passes on Node 24.21 | ros-builds | fixed on PR #187 |
| PR-066 | engineering | P2 | Lint and type errors fail on Vercel after the push, 10 builds on 2026-09-18; the pre-push check that catches them lives as an uncommitted dispatcher in the org hooks directory, since that repository sits on another session's branch | `scripts/pre-push-vercel-check.sh`, `scripts/install-git-hooks.sh`; `git status` in the org repository | Commit the dispatcher to AGFarms/agfarms-org when that checkout returns to its main branch | ros-builds | in_progress |
| PR-067 | patents | P1 | Research OS holds no patent record: the gateway has a USPTO ODP adapter on its org `main`, deployment unconfirmed and with no citations, and the graph has no patent nodes | `learning/research-os/PATENTS.md`, What the repositories hold | Import a CPC slice from the PatentsView bulk tables with the founder's ODP account, or from BigQuery, as patent nodes and `cites` edges | ros-patents 2 | open |
| PR-068 | patents | P2 | The 2026-05-03 licensing matrix and feed402 SPEC.md section 6.1 plan an EPO OPS query passthrough and WIPO content on the insight tier; the OPS terms forbid relaying data as such and WIPO resale needs a paid licence | `docs/PATENT_LICENSING.md`, Corrections of 2026-09-19 | Corrected in this repository on PR #188; the protocol change is feed402#12 and the registry change x402-research-gateway#68 | ros-patents 0 | in_progress: tracked in feed402#12 and x402-research-gateway#68 |
| PR-069 | security | P1 | The x402 research gateway builds the payment requirements it sends to the facilitator from the client's payload, never checks payee, amount, or asset against its route config, serves data when settlement fails, and keeps no record of payments already accepted; live on testnet, where no real money moves and the routes relay public APIs | `x402-research-gateway/internal/handler/handler.go`, `decodeAndVerifyPayment`; x402-research-gateway#67 | Build requirements from the route config, refuse mismatches and repeats, wait for settlement; required before any mainnet traffic | ros-patents 0 | open: tracked in x402-research-gateway#67 |
| PR-070 | patents | P2 | Research OS chat's `feed402_search_patents` tool calls the patents service with a stub payment header, asks `/citation?url=` where feed402 reads `canonical_url`, and expects `results` where the service answers `data.rows` | `src/lib/feed402-client.ts`, `src/app/api/chat/route.ts` | Move the chat tool's patent calls to the imported graph nodes, the free layer, in ros-patents 2 | ros-patents 2 | open |
| PR-072 | research-os | P2 | A teacher override upward awards the full XP ladder for that node with no ceiling and no review, once per node per learner. The audit row records it; nothing rate-limits it | Bucket critic ROS194-31 on PR #194, run against the local stack: an override to internalization on an untouched node returned awards true and paid 87 XP | Decide whether an upward override awards at all, and cap it if it does | `ros-ai-access` | open |
| PR-073 | privacy | P1 | `graph.level_overrides` carries `learner_id` and survives a privacy delete: `graph.privacy_delete_learner` covers eight learner-scoped tables and not this one | Bucket critic ROS194-32 on PR #194, read against migration 20260910040000 | Add the table to the delete set and to `PRIVACY_TABLES`, with a test that a deleted learner leaves no override row | `ros-ai-access` follow-up | open |
