# Files exempt from tools/hygiene/check-local-paths.py.
# One repo-relative path per line. See
# learning/research-os/compliance/REPO-HYGIENE-2026-09-11.md for the audit
# that produced this list and the reasoning for each entry. Add a new line
# only for a reviewed, deliberate exception, not to silence a real leak.

# Bead backup data: the .beads/ export mechanism writes these verbatim;
# rewriting them would corrupt the backup format. Untracking them is a
# founder decision (see the hygiene doc); they stay allowlisted until then.
.beads/backup/events.jsonl
.beads/backup/issues.jsonl

# Runner logs: raw captured terminal output from autonomous sessions.
# Rewriting a log falsifies the record of what actually ran.
_intake/.archive-runner.log
learning/.buildloop/run.log

# systemd unit files: paths here are load-bearing for the specific box the
# unit is installed on. Genericizing them would silently break the service.
_intake/sacred-history-corpus/runners/sacred-history-mirror.service.template
scripts/llm/systemd/bkt-llm-revtunnel.service
scripts/llm/systemd/bkt-llm-server.service
scripts/llm/systemd/bkt-llm-shim.service
scripts/systemd/canon-intake.service

# Narrative/meta docs: the /home/<user> text here is either quoting this
# same hygiene policy (describing what a leak scan checks for) or an
# operational history entry the founder has not yet decided to redact.
# Neither is a live leaked path in the sense this check exists to catch.
PRODUCTION_LOG.md
checkpoints/2026-05-03-session-checkpoint.md
_intake/research-os-k12/CHANGELOG.md
learning/research-os/CHANGE-LEDGER.md
tools/hypothesis-engine/docs/LOOP-LOG.md

# Test fixture: asserts the hypothesis-engine's own error-message
# sanitizer redacts /home/x and other absolute-root placeholders. The
# path is fixture input for that assertion.
tools/hypothesis-engine/tests/test_api.py

# This checker's own files: the hygiene doc names every leaked path it
# found, the checker's docstring gives an example, and the fixture test
# creates temp files containing /home/alice and similar strings on
# purpose, to prove the checker still catches them.
learning/research-os/compliance/REPO-HYGIENE-2026-09-11.md
tools/hygiene/check-local-paths.py
tools/hygiene/test-check-local-paths.sh
tools/hygiene/.local-path-allowlist
