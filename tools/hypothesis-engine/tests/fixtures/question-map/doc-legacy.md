Fixture Doc
===========

An intro paragraph that `apply_to_doc` must leave alone.

## Question map

A hand-written line this function's legacy path replaces on its first
run against a doc with no markers yet.

## Next section

Content after the question map that must survive every rewrite,
including a second consecutive one.
