# Test swarm findings, 2026-09-10

Property-test swarm over the stable `hte` modules (`hte/timeline.py`,
`hte/concepts.py`, `hte/address.py`, `hte/hypothesis.py`, `hte/evidence.py`,
`hte/belief.py`, `hte/unknowns.py`, `hte/export.py`, `hte/periods.py`,
`hte/retrieval.py`, `hte/paper.py`, `hte/referee.py`, `hte/publish.py`,
`hte/pipeline.py`, `hte/llm.py`), run with the `hypothesis` library. Every
finding below is pinned by a `pytest.mark.xfail(strict=True, ...)`
regression test in `tests/swarm/`, so the suite stays green
(`python3 -m pytest -q tests/swarm`) while the defect stays tracked.

**Status (2026-09-10): all six sub-findings resolved.** Every `xfail`
marker listed below has been removed; each regression test now asserts
the fixed behavior directly. See each finding's own **Resolution** note.

## FINDING-2026-09-10-001

- **Module**: `hte/timeline.py`
- **Property**: `relate(a, a) == AllenRelation.EQUAL` for every interval
  (reflexivity), and `converse(relate(a, b)) == relate(b, a)` for every
  pair.
- **Minimal failing input**: `Interval(start=7, end=7)` compared with
  itself (any zero-length interval against an equal zero-length interval).
- **Actual**: `relate` returns `MEETS`, not `EQUAL`.
- **Suspected cause**: `relate`'s branch order checks `a.end == b.start`
  (MEETS) and `b.end == a.start` (MET_BY) before checking `a.start ==
  b.start and a.end == b.end` (EQUAL). For a zero-length interval,
  `a.start == a.end`, so `a.end == b.start` is also true whenever `a` and
  `b` sit at the same point, and the MEETS branch fires first.
- **Fix suggestion**: move the EQUAL check (`a.start == b.start and a.end
  == b.end`) before the MEETS/MET_BY checks, or special-case
  `a.start == a.end == b.start == b.end` to return EQUAL directly.
- **Test**: `tests/swarm/test_timeline_props.py::
  test_relate_zero_length_interval_against_itself_should_be_equal`
- **Resolution**: `relate` now special-cases `a.start == a.end and
  b.start == b.end and a.start == b.start` to `EQUAL` before every other
  check, including the inherited `BEFORE`/`AFTER`/`MEETS`/`MET_BY` order.
  The Lean source (`Bucket.Timeline.relate`) keeps `meets`/`metBy` ahead
  of `equal` too and instead excludes this exact collision from
  `relate_converse_meets_metBy`'s converse proof via its `hnondeg`
  hypothesis, rather than resolving it to `equal`; this fix is a
  narrowly-scoped, documented Python-only divergence from that choice
  (not a Lean edit, and not a reordering of the general branch chain),
  chosen so `relate`'s own reflexivity and converse symmetry hold with no
  exclusion anywhere in the Python API. `test_relate_converse_symmetry_
  holds_for_general_pairs` no longer excludes the collision case, and a
  new `test_relate_of_any_zero_length_interval_against_itself_is_equal`
  generalizes the fixed example across the axis.

## FINDING-2026-09-10-002

- **Module**: `hte/belief.py`
- **Property**: `detectability_scale`'s own docstring claim, "as `delta ->
  0` the ratio approaches 1 and absence stops discriminating."
- **Minimal failing input**: `detectability_scale(1.0, 1e-9)`.
- **Actual**: returns `~1e-9` (approaches 0), not `~1`.
- **Suspected cause**: the docstring describes the intended asymptotic
  behavior of a likelihood ratio (near 1 = uninformative), but the
  implementation (`delta * e_raw_absence`) approaches 0 as `delta -> 0`,
  the opposite limit. The function's actual behavior (weight -> 0) is
  internally consistent with "absence stops discriminating" in the sense
  that the item stops contributing to either side of the pooled weight,
  but the docstring's own stated numeric limit ("approaches 1") does not
  match the formula.
- **Fix suggestion**: fix the docstring to state the formula's real limit
  (approaches 0, meaning the absence item's weight vanishes rather than
  its ratio going neutral), or change the formula to
  `1 - delta * (1 - e_raw_absence)` (or similar) if "approaches 1" was
  the intended contract all along. Flagging for the author to pick a
  side; this repository's own belief-model formulas come straight from
  `main.tex`, so the fix may belong upstream rather than in this package.
- **Test**: `tests/swarm/test_belief_props.py::
  test_detectability_scale_approaches_one_as_delta_approaches_zero_per_docstring`
  (renamed to `test_detectability_scale_matches_the_paper_equation_as_
  delta_approaches_zero`)
- **Resolution**: `main.tex`'s own `eq:detectability`
  (`e_absence = delta * e_raw-absence`) is the formula the implementation
  already carries out exactly; the docstring's claimed `delta -> 0` limit
  of 1 was the side that disagreed with the paper. The existing pinned
  test `tests/test_belief.py::test_detectability_scale_
  limits` already locked in `detectability_scale(0.8, 0.0) ==
  approx(0.0)`. Fixed the docstring to quote `eq:detectability` and state
  its real limit (the ratio, and the absence item's weight, both go to
  0 as `delta -> 0`; `e_absence == e_raw_absence` as `delta -> 1`). No
  change to `detectability_scale`'s arithmetic.

## FINDING-2026-09-10-003

- **Module**: `hte/belief.py`
- **Property**: `fuse` is associative for opinions sharing one base rate.
- **Minimal failing input**: `o1 = o2 = Opinion(b=1.0, d=0.0, u=0.0,
  a=0.0)` (fully dogmatic, agreeing), `o3 = Opinion(b=0.0, d=0.5, u=0.5,
  a=0.0)`. `fuse(fuse(o1, o2), o3) != fuse(o1, fuse(o2, o3))`.
- **Actual**: `fuse(o1, o2)` (both fully dogmatic) collapses to the
  vacuous opinion `(0, 0, 1, a)` regardless of whether `o1` and `o2`
  agree, discarding their shared verdict; folding in `o3` afterward then
  disagrees with folding `o3` into `o2` first (which does NOT hit the
  same zero-denominator branch, since `o2.u=0` but `o3.u=0.5`).
- **Suspected cause**: `fuse`'s zero-denominator guard (`if denom == 0:
  return Opinion(b=0.0, d=0.0, u=1.0, a=o1.a)`) fires whenever `o1.u ==
  o2.u == 0`, with no check for whether the two opinions agree. Two
  agreeing dogmatic opinions (`b=1,d=0` both) should plausibly fuse to
  themselves (`b=1,d=0,u=0`). Full uncertainty is defensible only for the
  disagreeing case (`b=1,d=0` vs `b=0,d=1`), which has no principled
  fused answer of its own.
- **Fix suggestion**: special-case `o1.b == o2.b and o1.d == o2.d` (and
  both `u == 0`) to return `o1` (or `o2`) unchanged, falling back to the
  vacuous opinion only when the two dogmatic opinions disagree.
- **Test**: `tests/swarm/test_belief_props.py::
  test_fuse_of_two_agreeing_dogmatic_opinions_should_preserve_their_verdict`
- **Resolution**: `fuse` now checks `o1.u == 0.0 and o2.u == 0.0` up
  front; when the two dogmatic opinions also agree (`o1.b == o2.b and
  o1.d == o2.d`), it returns that shared verdict unchanged (`u := 0`)
  rather than reaching the zero-denominator branch at all. Two dogmatic
  opinions that disagree still fall back to the vacuous opinion (`u :=
  1`), Jøsang's cumulative fusion having no principled fused answer for
  that case; the docstring now cites Jøsang's own dogmatic-opinion case
  by name. `test_fuse_is_associative_for_a_shared_base_rate` keeps its
  own `u > 1e-9` exclusion, since the disagreeing-dogmatic collapse is
  unfixed by design and still breaks associativity in that one branch.

## FINDING-2026-09-10-004

- **Module**: `hte/belief.py`
- **Property**: the vacuous opinion `(0, 0, 1, a)` is the identity for
  `fuse` under a shared base rate `a`, for every opinion.
- **Minimal failing input**: `o = Opinion(b=0.0, d=2.220446049250313e-16,
  u=0.9999999999999998, a=0.75)` fused with `vacuous = Opinion(b=0.0,
  d=0.0, u=1.0, a=0.75)`.
- **Actual**: `fused.b`, `fused.d`, `fused.u` all come out correct
  (matching `o`), but `fused.a == 1.0`, not `0.75`.
- **Suspected cause**: the combined-base-rate formula divides by
  `a_denom = u1 + u2 - 2*u1*u2`, guarded against division by zero only
  when `a_denom == 0` exactly. When `u1` is a handful of floating-point
  ULPs below `1.0` (a realistic value: `Opinion.from_evidence` produces
  exactly this shape whenever pooled evidence weight is enormous relative
  to `W`) and `u2 == 1.0`, `a_denom` is a few ULPs above zero rather than
  exactly zero, so the division runs anyway and catastrophically amplifies
  the rounding error already present in `u1`.
- **Fix suggestion**: widen the zero-denominator guard to a small
  epsilon (e.g. `abs(a_denom) < 1e-9`) rather than exact equality, falling
  back to the same `(o1.a + o2.a) / 2` branch in that near-degenerate
  window.
- **Test**: `tests/swarm/test_belief_props.py::
  test_fuse_vacuous_identity_is_numerically_unstable_just_below_u_equals_one`
  (renamed to `test_fuse_vacuous_identity_holds_just_below_u_equals_one`)
- **Resolution**: the `a_denom == 0` guard is now `abs(a_denom) <
  1e-9`. Inside that window, the combined base rate falls back to the
  mean of `o1.a`/`o2.a` weighted by each opinion's own confidence `1 -
  u` (an opinion at `u == 1` carries no information about its base rate
  and is weighted out), rather than a plain unweighted average; when
  both weights are zero too (`u1 = u2 = 1` exactly, the one case this
  window still reaches at the boundary), that reduces to the same plain
  average the old exact-zero branch already used, so `fuse(vacuous,
  vacuous)` is unaffected. `test_fuse_has_the_vacuous_opinion_as_
  identity`'s own `1e-6` exclusion band comfortably covers the new
  `1e-9` epsilon, so it needed no change.

## FINDING-2026-09-10-005

- **Module**: `hte/pipeline.py`
- **Property**: `run_pipeline`'s per-stage skip reason accurately
  describes why a stage was skipped, and its overall `"outcome"` reflects
  whether anything useful happened.
- **Minimal failing input**: `run_pipeline({"from_run":
  "/tmp/does-not-exist-abc123"})` (any `from_run` naming a directory with
  no `MANIFEST.json`).
- **Actual** (two sub-findings, one root cause):
  - **005a**: `emit_paper`/`referee`/`publish`'s own skip reason is the
    hardcoded string `"no usable run directory: from_run was not given
    and run_campaign did not produce one"`, printed even though
    `from_run` WAS given, just pointing at an invalid path. A caller
    debugging a typo'd `--from-run` reads a message that flatly denies
    they passed the flag.
  - **005b**: the top-level `"outcome"` field reads `"ok"`, not
    `"failed"`, even though every stage was skipped and nothing ran: each
    `_skipped_stage` call sets `ok=True` by design (a deliberate skip is
    not a failure), so `all(s.ok for s in stages.values())` is vacuously
    true when every stage skips, indistinguishable from a pipeline that
    legitimately had nothing to do.
- **Suspected cause**: the hardcoded reason string in `run_pipeline` (the
  `reason = "no usable run directory: ..."` line) does not branch on
  whether `cfg["from_run"]` was set at all; and `_skipped_stage`'s own
  `ok=True` convention, correct for an intentional skip (a period this
  package has no corpus loader for, say), gets reused for the "nothing
  in this pipeline ran at all" case with no distinguishing signal.
- **Fix suggestion**: interpolate the actual `cfg["from_run"]` value (or
  its absence) into the reason string; and either add a distinct
  `"outcome"` value (e.g. `"skipped"`) for the case where every stage
  skipped, or have `run_pipeline` itself treat "`from_run` given but
  unusable" as a hard failure rather than a stage skip.
- **Tests**: `tests/swarm/test_pipeline_props.py::
  test_missing_from_run_skip_reason_should_mention_the_path_that_was_given`
  (renamed to `test_missing_from_run_error_reason_should_mention_the_
  path_that_was_given`),
  `tests/swarm/test_pipeline_props.py::
  test_missing_from_run_outcome_should_not_read_ok`
  (renamed to `test_missing_from_run_outcome_should_read_error_not_ok`)
- **Resolution**: `run_pipeline` now validates `from_run` up front into a
  `from_run_error` string naming the actual path and precisely which way
  it failed (missing/not-a-directory vs. present but no `MANIFEST.json`).
  When set, `emit_paper` runs through a new `_failed_stage` helper
  (`ran=False, ok=False, error=from_run_error`, still writing its own
  `STAGE.json`) instead of `_skipped_stage`'s `ok=True` convention, and
  `referee`/`publish` cascade-skip off that failure the same way they
  already did for a real `emit_paper` exception. The non-`from_run` "no
  usable run directory" path (`run_campaign` legitimately produced
  nothing) is unchanged and still reads as a deliberate skip, since that
  case's own failure, if any, already surfaces through `run_campaign`'s
  own `ok`. The top-level `outcome` field gained a third value, `"error"`
  (bad input, distinct from `"failed"`, a stage that ran and raised),
  which fires whenever `from_run_error` is set; `"ok"` is no longer
  reachable through that path.
  **Cross-file follow-up (outside this fix's file scope):**
  `tests/test_pipeline.py::test_missing_manifest_in_from_run_skips_
  downstream_stages` pinned the exact bug this finding describes
  (`assert summary["outcome"] == "ok"  # every stage reports ok=True,
  just not run`); its two `"ok"` assertions were updated to `"error"` to
  match, since leaving them would fail the full suite against the
  corrected behavior this finding requires. No other assertion in that
  file changed.

## Summary by module

| Module | Findings |
|---|---|
| `hte/timeline.py` | 1 (001) |
| `hte/belief.py` | 3 (002, 003, 004) |
| `hte/pipeline.py` | 1 (005, two sub-findings) |
| `hte/concepts.py`, `hte/address.py`, `hte/hypothesis.py`, `hte/evidence.py`, `hte/unknowns.py`, `hte/export.py`, `hte/periods.py`, `hte/retrieval.py`, `hte/paper.py`, `hte/referee.py`, `hte/publish.py`, `hte/llm.py` | 0 |

## Round two

`bkt-hte-test-swarm`'s second pass of the property-test swarm, over the modules round one skipped:
`hte/corpus/education_atlas.py`, `hte/corpus/production.py`,
`hte/corpus/quantum_history.py`, `hte/synth.py`, `hte/fakellm.py`,
`hte/parallel.py`, `hte/batching.py`, `hte/link.py`, `hte/generate.py`,
in `tests/swarm2/`. Same convention: every finding below is pinned by a
`pytest.mark.xfail(strict=True, ...)` regression test, so
`python3 -m pytest -q tests/swarm2` stays green while the defect stays
tracked. One property this round's own task brief named,
`hte.link.link_evidence` never cross-linking the near-identical labels
`"Actor 5"`/`"Actor 10"`, `"planck"`/`"planck-1900"`, `"bell"`/
`"bell-inequality"`, was found ALREADY FIXED mid-run: `hte/link.py`
changed under this swarm partway through (another agent's own concurrent
work on `hte/link.py`, `hte/calibrate.py`, `hte/cli.py`, `hte/roles.py`,
`hte/runner.py`, `hte/tournament.py`), and the updated `slot_match_score`
now tokenizes labels and requires matching token counts before any
fuzzy-ratio credit, closing exactly this cross-link path
(`bkt-hte-linker-fuzzy-fix`, its own commit message). `tests/swarm2/
test_link_props.py` asserts the fixed behavior directly; no finding filed
for it.

### FINDING-2026-09-10-101

- **Module**: `hte/parallel.py`
- **Property**: `pmap`'s own docstring contract for `RateLimit`, "every
  worker pauses for a shared backoff window... before the item that
  raised it is retried, uncounted against its own retries budget," read
  against the stated purpose of `max_rate_limit_pauses`, "the exception
  keeps recurring." A batch of items each hitting `RateLimit` once and
  succeeding on their own next attempt should complete, since the
  rate limit is not, in fact, recurring for any one of them.
- **Minimal failing input**: 4 items, each raising `RateLimit` exactly
  once and succeeding on its own next attempt, `workers=1` (no
  concurrency involved), `max_rate_limit_pauses=3` (the default).
- **Actual**: `pmap` raises `RateLimitAborted` after 4 pauses, even
  though every item's own single retry would have succeeded.
- **Suspected cause**: `_RateLimitGate.pause()` increments one shared
  counter (`self._pauses`) per `RateLimit` EXCEPTION INSTANCE, from any
  item, for the lifetime of the whole `pmap` call, with no reset on an
  intervening success and no distinction between "the same still-active
  rate-limit window, observed again" and "a fresh, unrelated hit from a
  different item." Four different items each hitting it once, with
  nothing else ever failing, is enough to exceed the default budget of 3
  and abort the call outright, serially (`workers=1`), with no
  concurrency race involved.
- **Fix suggestion**: decrement (or reset) the pause count on an item's
  own successful completion after a pause, so the budget tracks "how deep
  into a currently-unresolved rate limit are we." Short of a code change,
  `max_rate_limit_pauses`'s own documentation could state plainly that it
  counts every occurrence for the life of the whole call, so a caller
  batching many items sizes it proportional to item count, matching how
  many consecutive pauses one stuck rate limit alone would need.
- **Test**: `tests/swarm2/test_parallel_props.py::
  test_finding_2026_09_10_101_transient_per_item_rate_limits_should_not_abort_the_whole_call`

### FINDING-2026-09-10-102

- **Module**: `hte/corpus/quantum_history.py`
- **Property**: this round's own generic corpus-adapter property, "stemma
  edges that resolve": every `Source.stemma_parents` entry should name a
  `Source` present in the same ingested `Corpus`.
- **Minimal failing input**: one card, `T-real.md`, whose `## Sources`
  section names `T-ghost` (a doc id with no corresponding `T-ghost.md`
  file anywhere in the ingested `corpus_dir`).
- **Actual**: `corpus.sources["T-real"].stemma_parents == ["T-ghost"]`,
  and `"T-ghost" not in corpus.sources`: a dangling stemma edge.
- **Suspected cause**: `_parse_card`'s `stemma_parents` are built by
  `_CROSS_REF_RE.findall(line)` over the `## Sources` section's own free
  prose, matching ANY `T-[a-z0-9]+`-shaped token (excluding only an exact
  self-reference to the card's own `doc_id`), with no check anywhere that
  the matched id names a card the corpus ingested. The real
  shipped `quantum/07-history/` corpus happens to carry no dangling
  reference today (confirmed empirically: `qh.ingest()` over the real
  corpus finds zero dangling stemma parents out of 16 sources), since its
  own "Go deeper" cross-references are curated to point at real sibling
  cards, but nothing in the parser enforces that; a future card added to
  that corpus citing a card by a wrong or since-renamed id, or an ingest
  over a partial subdirectory of the corpus, would silently carry the
  identical dangling edge.
- **Fix suggestion**: after every card is parsed and `sources` is fully
  built, filter (or warn on) each `Source`'s own `stemma_parents` against
  the final `sources` dict's own keys, dropping (or flagging) any
  reference that does not resolve, the same "resolves, or is excluded"
  contract `education_atlas.py`'s own OWID-stemma rule already applies at
  construction time (`_OWID_DOCUMENTED_ORIGIN_DATASET` checked against
  `pair_set` before ever being added).
- **Test**: `tests/swarm2/test_corpus_quantum_history_props.py::
  test_finding_2026_09_10_102_cross_reference_to_a_nonexistent_card_should_not_dangle`

### Summary by module, round two

| Module | Findings |
|---|---|
| `hte/parallel.py` | 1 (101) |
| `hte/corpus/quantum_history.py` | 1 (102) |
| `hte/corpus/education_atlas.py`, `hte/corpus/production.py`, `hte/synth.py`, `hte/fakellm.py`, `hte/batching.py`, `hte/link.py`, `hte/generate.py` | 0 |

## Round three

`bkt-hte-test-swarm`'s third pass of the property-test swarm, over the
three least-covered files `tests/COVERAGE.md` named for the next pass:
`hte/cli_synth.py` (24.1%), `hte/cli.py` (90.1%), `hte/roles.py` (90.7%),
in `tests/swarm3/`. Same convention: every finding below is pinned by a
`pytest.mark.xfail(strict=True, ...)` regression test, so `python3 -m
pytest -q tests/swarm3` stays green while the defect stays tracked. This
round touched no source file and no run directory (two live campaigns,
`runs/production/` and `runs/education-atlas/`, and one concurrent
agent's own edits to `hte/runner.py`, `hte/paper.py`, `hte/referee.py`,
`hte/publish.py`, `hte/pipeline.py`, and a new `hte/artifacts.py`, were
both off limits). Both findings below are in `hte/cli_synth.py`, the file
this round's own task brief singled out as least covered; neither
`hte/cli.py` nor `hte/roles.py` turned up a defect this round.

**Status (2026-09-10): both findings resolved.** Both `xfail` markers
below have been removed; each regression test now asserts the fixed
behavior directly. See each finding's own **Resolution** note.

### FINDING-2026-09-10-201

- **Module**: `hte/cli_synth.py`
- **Property**: every other bad-CLI-input path in this package's own two
  CLIs exits 2 with a usage message rather than crashing (`hte/cli.py`'s
  `--corpus` choices via argparse; `hte-synth`'s own `sweep --param` and
  `run --seeds` are documented, free-text flags with no argparse
  `choices=` of their own, so this property has to hold by explicit
  validation inside `_cmd_run`/`_cmd_sweep` instead).
- **Minimal failing input**: `hte-synth run --seeds abc --out <dir>` (any
  spec `_parse_seed_range` cannot parse: `"abc"`, `"1-"`, `"-"`,
  `"1-2-3"`, `"1.5"`, `"1, x"` all reproduce it).
- **Actual**: `main()` raises a raw `ValueError` (`invalid literal for
  int() with base 10: 'abc'`): an uncaught Python exception with a full
  traceback on stderr, in place of `SystemExit(2)` and a usage message.
- **Suspected cause**: `_cmd_run`/`_cmd_sweep` call `_parse_seed_range
  (args.seeds)` directly with no `try`/`except` around it; `_parse_seed_
  range` itself has no usage-message contract of its own (a plain
  internal helper, raising `ValueError` for malformed input as expected,
  confirmed by this round's own `test_parse_seed_range_malformed_spec_
  raises_value_error`), so the gap is that nothing between it and
  `main()` ever catches that exception and turns it into the
  `parser.error(...)` (`argparse`'s own `SystemExit(2)` plus a `usage:`
  line) every `argparse`-`choices`-backed bad input in this package gets
  for free.
- **Fix suggestion**: wrap the `_parse_seed_range(args.seeds)` call in
  each of `_cmd_run`/`_cmd_sweep` in a `try/except ValueError as exc:
  parser.error(f"--seeds: {exc}")` (needs `parser` threaded into the
  command function, or the catch moved up into `main()` around
  `args.func(args)`), matching `argparse`'s own two-line convention: a
  `usage:` line to stderr, then `hte-synth run: error: ...`, exit 2.
- **Test**: `tests/swarm3/test_cli_synth_props.py::
  test_finding_2026_09_10_201_malformed_seeds_should_exit_two_with_usage`
- **Resolution**: a new `_parse_seeds_or_error(parser, spec)` helper wraps
  `_parse_seed_range(spec)` in `try`/`except ValueError`, calling
  `parser.error(...)` (naming the accepted forms) on any unparseable
  spec, and separately calls `parser.error(...)` when the spec parses
  clean but names no seeds at all (`""`, `","`), a second crash-free gap
  the same root cause left open. `_cmd_run` and `_cmd_sweep` both call
  this helper instead of `_parse_seed_range` directly; `build_parser`
  threads each subcommand's own subparser into `args.parser` via
  `set_defaults(parser=p_run)` / `set_defaults(parser=p_sweep)`, so
  `parser.error()`'s usage line and `prog` name the right subcommand
  (`hte-synth run: error: ...` / `hte-synth sweep: error: ...`), matching
  this finding's own fix suggestion. `_parse_seed_range` itself is
  unchanged, including its own documented reversed-range form (`"3-0"`,
  descending); only the CLI entry point's handling of what that function
  raises (or silently returns empty) changed.

### FINDING-2026-09-10-202

- **Module**: `hte/cli_synth.py`
- **Property**: same as FINDING-2026-09-10-201, applied to `sweep
  --param`: an unregistered parameter name should exit 2 with a usage
  message rather than crash.
- **Minimal failing input**: `hte-synth sweep --param not_a_real_kwarg
  --values 0,1 --seeds 0 --out <dir>` (any `--param` value that is not
  one of `hte.synth.make_world`'s own keyword arguments).
- **Actual**: `main()` raises a raw `TypeError` (`make_world() got an
  unexpected keyword argument 'not_a_real_kwarg'`), the instant the first
  seed's `world_kwargs = {**synth.SMALL_WORLD_KWARGS, args.param: value}`
  dict reaches `synth.make_world(seed, **world_kwargs)` inside
  `run_one_seed`, in place of `SystemExit(2)` and a usage message.
- **Suspected cause**: `--param`'s own `argparse.add_argument` call
  carries no `choices=` (unlike `hte/cli.py`'s `--corpus`), by design per
  its own help text, `"a hte.synth.make_world kwarg name, e.g. noise"`,
  since the valid set is `make_world`'s own keyword-argument list rather
  than a small fixed enum this module wants to hardcode and keep in
  sync. Nothing in `_cmd_sweep` validates `args.param` against
  `inspect.signature(synth.make_world).parameters` (or against
  `synth.SMALL_WORLD_KWARGS`'s own keys, the "documented" sweepable
  subset this round's own task brief points at) before handing it to
  `make_world` as a keyword argument, so an unregistered name's first
  sign of trouble is the callee's own `TypeError`, one full campaign
  setup after the flag was parsed.
- **Fix suggestion**: validate `args.param` in `_cmd_sweep`, before the
  seed loop starts, against `inspect.signature(synth.make_world).
  parameters` (accepting anything `make_world` itself would accept,
  matching the help text's own claim) or against `synth.
  SMALL_WORLD_KWARGS`'s keys (the narrower, "this is what `sweep` is
  meant to vary" reading), calling `parser.error(...)` on a miss the
  same way FINDING-2026-09-10-201's own fix would for `--seeds`.
- **Test**: `tests/swarm3/test_cli_synth_props.py::
  test_finding_2026_09_10_202_unknown_sweep_param_should_exit_two_with_usage`
- **Resolution**: a new `_make_world_param_names()` reads
  `inspect.signature(synth.make_world).parameters` at call time (every
  keyword-capable parameter but `seed`), the broader of the two readings
  the fix suggestion offered, matching `--param`'s own help text claim
  ("a `hte.synth.make_world` kwarg name") rather than narrowing to
  `SMALL_WORLD_KWARGS`'s smaller preset-override subset. `_cmd_sweep`
  checks `args.param` against that list before the seed loop starts and
  calls `parser.error(...)`, naming the registered params, on a miss.
  `--param`'s own help text now lists the same registered names, built
  from the identical function, so `--help` output and the runtime check
  cannot drift apart. `--seeds`/`--values` malformed input inside `sweep`
  goes through the same `_parse_seeds_or_error` helper and a matching
  `try`/`except ValueError` around `_parse_values`, closing the identical
  gap `sweep` shared with `run` for those two flags.

### Summary by module, round three

| Module | Findings |
|---|---|
| `hte/cli_synth.py` | 2 (201, 202) |
| `hte/cli.py`, `hte/roles.py` | 0 |

## Round four

Not a scheduled swarm pass: this finding surfaced while running `make
test` (the every-commit gate, `-x`, stops at first failure) for the
`feat/hte-calibration-vocab` bead's own vocabulary-induction and
calibration-fit work, against `hte/corpus/literature.py`, a module round
two's own file list did not cover. Filed and resolved the same day it was
found, matching round one's and round three's own "found, fixed, pinned
by a regression test" convention; no `xfail` marker was ever needed since
the fix landed before this file did.

### FINDING-2026-09-10-301

- **Module**: `hte/corpus/literature.py`
- **Property**: every card file `hte.corpus.literature.load_raw` reads
  should parse, given a well-formed `PLAN.md`-shaped frontmatter block
  anywhere near the top of the file.
- **Minimal failing input**: any card file whose first line is a
  `voice-ignore-file` HTML comment (`CLAUDE.md`'s own escape hatch for
  verbatim material, `<!-- voice-ignore-file: ... -->`) ahead of the
  frontmatter's own opening `---` line. As of 2026-09-10, all 6 of this
  module's own fixture cards under `hte/data/literature-fixtures/` carry
  exactly this marker.
- **Actual**: `make test` (`tests/test_corpus_literature.py`'s own
  module-scoped `cards` fixture) crashed at collection with `ValueError:
  literature adapter: <path> has no frontmatter opening \`---\`` for
  every one of the 6 fixture cards, stopping the whole suite at the
  first failing test (`-x`) before any test past it in file-name order
  could run at all.
- **Suspected cause**: `_parse_frontmatter`'s own `raw.startswith("---
  \n")` check assumes the frontmatter opener is the file's own first
  four bytes, written before any fixture carried a leading comment ahead
  of it. Every one of the 6 shipped cards has since been annotated with
  a `voice-ignore-file` marker (an org-wide voice-lint pass over the
  whole tree, run some time after this module's own last edit landed),
  so the assumption this check encoded stopped holding for 100% of this
  module's own fixture data without any change to `hte/corpus/
  literature.py` itself.
- **Fix suggestion**: tolerate a leading run of blank lines and
  single-line `<!-- ... -->` comments before the frontmatter's own
  opening `---`, adjusting the line-number and char-offset shift
  `_parse_frontmatter` already applies (`fm_lines = [(lineno + 1, offset
  + 4, line) ...]`) to account for however many preamble bytes and lines
  were skipped, so `Claim.char_start`/`char_end`/`line_start`/`line_end`
  still land on the right offsets in the file's own full, unmodified text.
- **Test**: `tests/test_corpus_literature.py::
  test_leading_voice_ignore_file_comment_before_frontmatter_is_tolerated`
- **Resolution**: a new `_skip_leading_preamble(raw)` returns how many
  leading bytes and lines match blank-or-`_LEADING_COMMENT_RE` before
  the first line that does not; `_parse_frontmatter` slices `raw` past
  that preamble before checking for the `---` opener, and shifts
  `fm_lines`' own line numbers and char offsets by `preamble_lines + 1`
  and `fm_start` (the preamble's own length plus the opener's four
  bytes) respectively, in place of the old fixed `+1`/`+4`. A card with
  no preamble at all (the common case for every other adapter's own
  fixtures, and for any future literature card with no marker) skips
  zero bytes and zero lines, so this reading is additive: the old fixed
  shift is exactly the `preamble_lines == 0` case of the new one, folded
  into the same one code path. Verified against all 6 real fixture cards
  (`tests/test_corpus_literature.py`'s own full suite, including the
  char-offset-anchoring test already checking the real `bloom-1984`
  fixture file) and a synthetic regression test built directly against
  `_parse_frontmatter`.
