# Research OS for K-12: Preregistration Draft

**Status:** draft, revision 1, bead `ros-08` · **Date:** 2026-09-10 · Reads against `learning/research-os/PLAN-REVISION-1.md` sections 2c, 3 item 9, and 4; `learning/research-os/PLAN-REVISION-3.md` section 2; `learning/research-os/LEARNER-STATE-MODEL.md` sections 4 and 5; `src/lib/research-os/EVIDENCE-SCHEMA.md`; `learning/research-os/GUIDANCE.md`; `learning/research-os/PRODUCTION-GUARD.md`; `_intake/research-os-k12/04-compliance-distribution.md` section 10; `_intake/research-os-k12/RESEARCH-OS-K12-SYSTEM-REVIEW.md` section 9; `_intake/research-os-k12/OVERLAP-RESEARCH-OS-AND-AI-FOR-RESEARCH.md`; `_intake/research-os-k12-literature/project-based-inquiry-learning/chen-yang-2019-pbl-meta-analysis.md`, `furtak-et-al-2012-inquiry-based-science-meta-analysis.md`, `lazonder-harmsen-2016-inquiry-based-learning-meta-analysis.md`, and `kirschner-sweller-clark-2006-minimal-guidance-does-not-work.md`; `_intake/research-os-k12-literature/student-research-experiences/grinnell-et-al-2020-science-fair-outcomes.md`; `_intake/research-os-k12-literature/source-evaluation/wineburg-mcgrew-2019-lateral-reading.md`; `_intake/research-os-k12-literature/hci-human-ai-collaboration/bucinca-malaya-gajos-2021-cognitive-forcing-functions.md` and `doshi-hauser-2024-generative-ai-creativity-diversity.md`.

This is a draft to be filed on OSF once the IRB-of-record partner named in `IRB-PACKET-OUTLINE.md` is confirmed and the Phase 1 pilot site is set (`PLAN-REVISION-1.md` blocking decision 1). No IRB approval exists yet, and no partner school or district is under contract. Every claim below that depends on either is written as a plan awaiting confirmation.

## Revision history

**Revision 1 (2026-09-10).** Grounded in `PLAN-REVISION-3.md` section 2's evidence-driven revisions, against `learning/research-os/GUIDANCE.md` (branch `feat/ros-faded-guidance`, bead `ros-14`, no PR opened yet) and `learning/research-os/PRODUCTION-GUARD.md` (PR #73, open, not yet merged to `main`).

1. Effect-size anchoring: the Sampling Plan's own d = 0.4 to 0.5 planning assumption moves from one unsourced heuristic and one percentage-only contrast to three named meta-analytic anchors (Chen and Yang 2019, Furtak and colleagues 2012, Lazonder and Harmsen 2016), each checked against this design's own population and moderator fit rather than cited only for direction; the naive and cluster-corrected n-per-arm tables are recomputed against that anchoring.
2. Arms and factors: the forcing arm switch and the guidance arm switch are registered as two class-level factors fixed on for every Phase 1 class rather than crossed factorially, with `guidanceLevel` entered as a pre-specified live-learner covariate and `forcingEnabled` logged as a data-integrity check.
3. Two new pre-specified secondary outcomes: a calibration outcome under H1 (stated confidence against source-prediction correctness, from the cognitive-forcing record) and a production provenance-flags-rate outcome under H3 (unverified sources, duplicate flags, missing counter-evidence, from the production guard). A lateral-reading outcome is considered and deferred, since the field it would read does not exist in shipped code.
4. A required-participation and misconduct-risk clause: Production submission stays opt-in per `PLAN-REVISION-3.md` decision 6, and a class whose teacher requires it anyway is stratified into the model as a covariate rather than excluded outright, citing Grinnell and colleagues (2020).
5. `INSTRUMENTS.md` gains two new sections for the guidance-level field and the production provenance flags; `RESEARCH-QUESTIONS.md` gains five append-only pointer lines.

Every sentence this revision replaces is preserved verbatim in `_intake/research-os-k12/DELETIONS.md`. No partner, approval, or IRB status named anywhere in this draft changed in this revision.

## Template and its source

The section structure below (Study Information, Design Plan, Sampling Plan, Variables, Analysis Plan, Other) follows the OSF standard preregistration form, whose content traces to van 't Veer and Giner-Sorolla's "Pre-Registration in Social Psychology, a Discussion and Suggested Template" (*Journal of Experimental Social Psychology* 67, 2016, doi:10.1016/j.jesp.2016.03.004), the paper OSF's own registries page credits as the source of its standard template. This pass attempted nine direct WebFetch calls against `osf.io` and its registries pages to confirm the live section order; every attempt returned either the site's client-rendered shell with no template text (`osf.io` renders its registration forms in JavaScript, which a markdown-converting fetch cannot read), a 404 on a guessed static path, or a paywalled publisher redirect for the source paper itself. The section order below is sourced to the published template paper rather than a confirmed live scrape. **Before this draft is filed on OSF, cross-check the live form at osf.io/registries against the section order below and adjust field-by-field; do not submit unverified.**

---

## Study Information

### Title

Scoped-tool AI support and frontier-backward routing in a K-12 knowledge-graph workspace: a three-arm pilot.

### Research questions and hypotheses

The study registers six primary, directional hypotheses, each drawn from one of the twelve overlap-map questions in `OVERLAP-RESEARCH-OS-AND-AI-FOR-RESEARCH.md` and selected because `PLAN-REVISION-1.md` section 4 marks it answerable, in whole or in a first-signal partial form, inside a single Phase 1 pilot rather than requiring district scale. The six questions left out of this registration (overlap-map questions 2, 4, 7, 10, 11, 12) are named at the end of this subsection with the reason each waits.

**H1, extends overlap-map question 1, `RESEARCH-QUESTIONS.md` Q8 and Q11.** On a delayed (eight-week), unassisted near-transfer post-test, learners assigned to the constrained find-quote-check-organize workspace will score higher than learners assigned to a full open-ended chatbot arm (H1a), and will not score lower than learners assigned to the no-AI control arm (H1b). Grounded in Bastani and colleagues (2025), whose guardrailed hint-only tool preserved most of a practice-performance gain with no significant drop in unassisted exam performance while an unrestricted tool produced a significant drop; in Kosmyna and colleagues (2025), whose EEG measure and later-recall measure both showed the largest cost in the least-constrained condition; and in Risko and Gilbert's (2016) offloading framework, which the three arms are built to separate. A secondary, exploratory outcome under H1, scoped at Phase 1 to a first descriptive signal instead of confirmatory power (`PLAN-REVISION-1.md` section 2c), is class-level production diversity: pairwise similarity across a class's accepted productions on the same routed target, scored the way Doshi and Hauser (2024) score similarity between AI-assisted stories, predicted to be higher (less diverse) in the full-chatbot arm than in the constrained-workspace arm, with the loss concentrated in learners who adopt a tool suggestion directly rather than revising it, per Doshi and Hauser's own finding.

**H2, extends overlap-map question 3, `RESEARCH-QUESTIONS.md` Q5 and Q29.** Within a class, learners randomly assigned a frontier-backward route to a chosen target node will reach Understanding on that target in fewer sessions, and in less elapsed time on task, than learners assigned Academy's forward, reach-weighted default route to the same target. Grounded in Pan and colleagues (2017), Liang and colleagues (2018), Roy and colleagues (2019), and Gasparetti and colleagues (2017), the direct precedent for inferring a prerequisite edge and walking it backward to build a route; and in Barnett and Ceci (2002), whose transfer taxonomy is the framework for stating that this is a near-transfer claim (a template-plus-scenario item varying surface features on the same practiced construct) rather than an undifferentiated one.

**H3, extends overlap-map question 5, `RESEARCH-QUESTIONS.md` Q13 and Q30.** Embedded within the subset of learners across all three arms who submit an accepted production eligible for a citation event, at least one week's self-explanation quality (scored on the rubric in `LEARNER-STATE-MODEL.md` section 4's Understanding measure) will not differ between a recognition-only arm (an Open Badges 3.0 credential issued on acceptance, no ledger entry) and a recognition-plus-ledger arm (the same badge plus a custodial ledger entry with no liquid payout). Grounded in Deci, Koestner, and Ryan (1999), whose 128-study meta-analysis names tangible, expected, performance-contingent rewards, the category a citation-on-acceptance payment falls into, as the reliable case for undermining intrinsic motivation, and in Gneezy and Rustichini (2000), whose field and lab evidence is that a small payment can perform worse than no payment at all. Neither source predicts harm from a recognition-only or non-liquid-ledger design; Mekler and colleagues (2017) found a non-payment, recognition-style mechanic raised output with no measured motivational cost, the direct precedent for expecting no difference between these two arms. This hypothesis explicitly excludes a live-cash arm, which `04-compliance-distribution.md` section 6 states needs an employment-law opinion and custodial-account infrastructure neither of which exists.

**H4, is overlap-map question 6 and `RESEARCH-QUESTIONS.md` Q22, run offline against the engine's held-out corpus before any pilot learner is involved.** The Check tool will show lower agreement with human-labeled verdicts when judging a claim against an isolated quoted span than when judging the same claim against full-document context. A second, exploratory sub-hypothesis under H4, motivated by Binz and Schulz (2023)'s finding that small, meaning-preserving phrasing changes swing a model's reasoning accuracy: Check's verdict will show lower within-claim agreement between the claim as originally phrased and one meaning-preserving paraphrase than between two independent runs on the identical, unparaphrased claim.

**H5, extends overlap-map question 8, `RESEARCH-QUESTIONS.md` Q19 and Q24, run engine-side on productions from Phase 1's own campaign run once `ros-12`'s wiring lands and at least one production reaches `accepted` status.** Holdout calibration of the engine's ranked hypotheses, measured against later-confirmed ground truth, will be closer to observed outcome frequency (lower Brier score, or a calibration curve closer to the identity line) when the corpus includes reviewed, teacher-signed-off Production-tier evidence than when the same corpus runs without those productions. Grounded in Macnamara and colleagues (2024), whose finding that learners cannot self-assess their own AI-assisted skill decay argues sign-off cannot be self-reported, and in Schmidgall and colleagues (2025), whose Agent Laboratory found intermediate-stage human feedback improved output quality more than end-only review, the basis for expecting the reviewed-and-signed-off tier to carry more calibration value than an unreviewed submission would.

**H6, extends overlap-map question 9, `RESEARCH-QUESTIONS.md` Q2 and Q33.** On the eight-week retention probe (`INSTRUMENTS.md`), learners whose review items are scheduled at FSRS-computed spaced intervals will show a higher pass rate than learners whose review items are scheduled at a fixed, massed interval, replicating the testing-effect direction Roediger and Karpicke (2006) establish. A moderator sub-hypothesis, exploratory rather than confirmatory since no batch-two paper tests it directly (`OVERLAP-RESEARCH-OS-AND-AI-FOR-RESEARCH.md` question 9's own evidence note): the FSRS-versus-fixed advantage will be smaller on the machine-graded (Check-tool) checkpoint than it would be expected to be on a human-graded checkpoint, motivated by Stadler, Bannert, and Sailer (2024)'s finding that LLM assistance during inquiry lowers effort and depth together, a pattern that could mask part of the same depth difference a human grader would catch.

**Questions left out of this registration.** Overlap-map question 2 (two-sigma-class effect at graph-routed scale) is district-scale by construction; a pilot-scale point estimate is recorded as exploratory only. Question 4 (AI-generated and K-12 productions meeting a domain-scientist novelty bar) needs more accepted-Production-tier volume than one pilot semester of the rarest of the five states will produce (`PLAN-REVISION-1.md` section 4, row 1 and row 4). Question 7 (literature-based discovery closing gap nodes) is blocked on `ros-12`'s engine-side `GapNode`/`value_of_information` wiring, independent of pilot scale. Question 10 (division of cognitive labor across states, by age) needs a contrasting age band a single grade-banded pilot cannot supply. Question 11 (burden of knowledge predicting gap-node clustering) is explicitly generational and needs longitudinal engine-run data no single semester carries. Question 12 (scientific understanding distinct from correctness) needs a Binz-and-Schulz-style probing battery run on enough Production-tier cases for construct validation, more than one pilot semester's rarest-state volume supplies; a first, exploratory pass on whatever Production-tier cases the pilot does produce is logged as exploratory under H5's own data collection instead of standing as a sixth confirmatory hypothesis.

---

## Design Plan

### Study type

Experiment, an experimental manipulation is fully controlled by the research team (route and workspace tool access), and no data collection has begun.

### Blinding

Participants (learners) are not blinded to which of the three workspace arms they use, since the tool itself is visibly different across arms. Human raters scoring Understanding and Internalization transfer items (the inter-rater procedure below) are blind to the learner's arm assignment where the rated artifact does not itself reveal it (an explanation or a transfer-item answer carries no arm label); teacher reviewers approving or returning a production are not blinded to arm, since the teacher's own class roster and the workspace the student used are visible in the ordinary course of review, a limitation stated here rather than concealed. The second rater in the inter-rater sampling procedure is blind to the first rater's decision and reason, per `LEARNER-STATE-MODEL.md` section 4.

### Study design

A three-arm, class-cluster-randomized design. Arm assignment happens at the class level (every learner in a given class period uses the same workspace arm), because the four-tool workspace, the full chatbot, and the no-AI condition are each a distinct classroom environment a teacher runs for the whole section, with no per-student toggle between them; this is also the unit `PLAN-REVISION-1.md` section 4 and `04-compliance-distribution.md` section 10 both describe when they name "students nested in classrooms" as the clustering structure a proper power analysis has to account for. H2's route-assignment comparison (frontier-backward versus forward) is the one exception: it randomizes at the individual-learner level within a class, per `RESEARCH-QUESTIONS.md` Q5's own design, since both routes can coexist inside the same constrained-workspace arm without contaminating the arm-level AI-access manipulation.

| Arm | What the learner has access to | Primary role |
|---|---|---|
| Constrained AI (the product) | Locate and Quote (retrieval only, no model call), Check (Sonnet-tier, verifies a claim against cited evidence, abstains on weak retrieval), Organize (Haiku-tier, restructures notes into claim-evidence-source shape); no tool ever writes the learner's answer | Tests H1a and H1b directly; the study's own workspace |
| Full chatbot | Open-ended generative AI assistance on the same source material, no tool-boundary restriction | Tests the hypothesized failure mode: short-term ease, worse independent performance once the tool is withdrawn |
| No AI | Same knowledge-graph navigation UI and source material, human-only research tools, no AI assistance of any kind | Baseline |

### Guidance and forcing factors

Two more class-level switches exist on top of the three arms above, both already built: the forcing arm switch (`graph.classes.forcing_enabled`, nullable, defers to the `RESEARCH_OS_FORCING_ENABLED` env var when unset, PR #63, merged to `main`) and the guidance arm switch (`graph.classes.research_os_guidance_enabled`, not-null, default true, built on branch `feat/ros-faded-guidance`, bead `ros-14`, no PR opened yet). Both switches act only inside the constrained-AI arm's own Check tool; the full-chatbot and no-AI arms carry no Check tool for either switch to affect, so forcing and guidance are not factors that cross meaningfully against arm itself, only within the constrained-AI arm's own allocation.

**The design choice: both factors fixed on.** Phase 1 fixes both switches on for every class rather than running a full factorial or a fractional slice of one within the constrained-AI arm. This draft's own "What Phase 1's feasible enrollment can detect" below already finds the base three-arm contrast underpowered at Phase 1's feasible four-to-six-class enrollment; splitting the constrained-AI arm's own two-to-three classes further, across four factorial cells or even a two-cell fractional slice, would leave most cells below the five-consented-learner integrity floor the Data exclusion section sets, and would let whichever teachers happen to land in one cell confound the factor itself, the class-level confound the cluster-randomized design exists to prevent. Both switches also default on in shipped code (`RESEARCH_OS_FORCING_ENABLED` defaults on when unset; `research_os_guidance_enabled` defaults true), so fixing both on for Phase 1 tests the product as it ships by default. A factorial or fractional extension of this same registration is a named candidate for Phase 2, once the district-scale class counts in the cluster-corrected table below are met; its own n table is not pre-specified here.

**What is logged regardless of the fixed-on choice.** Every `check` evidence event already carries `forcingEnabled` (`EVIDENCE-SCHEMA.md`); once branch `feat/ros-faded-guidance` merges, every such event will also carry `guidanceLevel` (`high` | `medium` | `low`), per `GUIDANCE.md` section 4. `guidanceLevel` enters H1's and H2's live-learner models as a pre-specified covariate rather than a manipulated factor (Analysis Plan, Covariates, below), since Phase 1's own fixed-on choice leaves the field's variation coming from the learner's adaptive base-level-plus-fading rule rather than from an experimental assignment. `forcingEnabled` is logged on every event but not entered as a model covariate or factor in Phase 1, since a switch fixed on for every class carries no variance to test; it stands as a data-integrity check confirming every check event in the confirmatory dataset ran through the forcing pathway. A Phase 2 revision that crosses either switch experimentally reads Phase 1's own fixed-on data back as a baseline rather than starting from nothing.

### Randomization

Class-level random assignment to arm, stratified by grade band and by gifted-versus-Title I outreach channel (`04-compliance-distribution.md` section 12's equity plan, pairing every gifted-program pilot with a Title I pilot in the same outreach cohort), using a random-number-generator seed recorded in the study's own OSF component before assignment, so the sequence is reproducible and auditable. Where a single teacher teaches multiple sections of the same course, sections are treated as separate classes for randomization purposes and the teacher's own identity is entered as a covariate (a teacher effect nested under the class random effect rather than crossed with it) in the Analysis Plan below. Individual-level randomization for H2's route comparison is block-randomized within each class so every class contributes learners to both route conditions.

---

## Sampling Plan

### Existing data

None. No data collection has begun, and none of the learners, classes, or productions named anywhere in this draft or in any Research OS document exist as enrolled study participants as of this draft's date.

### Data collection procedures

Recruitment follows `04-compliance-distribution.md` section 9's ranked procurement paths; the pilot targets a district-MOU or charter-network path (path 4 or path 6) rather than the fastest teacher-led or homeschool paths, since a research-grade cluster-randomized design needs several classes assigned as intact units under one coordinating agreement rather than several independent, self-selecting individual teachers. Consent and assent follow `IRB-PACKET-OUTLINE.md`. Enrollment closes before the semester's route and arm assignments are drawn; a learner who enrolls after randomization has already run joins the study only as an observed non-randomized case, logged and excluded from the confirmatory analysis, never assigned to an arm retroactively.

### Required participation and misconduct risk

`PLAN-REVISION-3.md` decision 6 sets the opt-in default this Sampling Plan follows: a class's own use of the routed workspace can be an ordinary part of a class period a teacher already runs, the same as any other assigned classwork, but submitting a Production stays opt-in until the production provenance guard (`PRODUCTION-GUARD.md`, branch `feat/ros-production-guard`, PR #73, open, not yet merged to `main`) reaches `main`. No citation payment or other incentive attaches to a Production before that guard exists, independent of whether a given class treats Production submission as required or opt-in.

Grinnell and colleagues (2020) supply the reason this Sampling Plan checks every class's own stated opt-in status rather than treating it as settled once recorded: among students surveyed nationally who said they had no interest in a science or engineering career and were required to participate in science fair anyway, about 10 percent reported research misconduct such as plagiarism, a higher rate than among students who chose to participate. The mechanism Grinnell and colleagues measure, required participation combined with disinterest, needs no payment to operate; a required, graded Production carries the same mechanism whether or not the citation-payment guard above has shipped.

**How a class that requires Production submission anyway is handled.** A teacher's own requirement status for Production submission, `required` or `opt-in`, is recorded once per class at enrollment (`INSTRUMENTS.md` section 6 below adds the field). A required-Production class is not excluded from the study: Bangera and Brownell (2014)'s own access argument, that a required, structured research experience reaches every enrolled student while a purely voluntary one reaches only the subset who opt in, argues excluding required classes would bias the sample toward volunteer-only teachers and students, working against the same access case `PLAN-REVISION-3.md` section 2b already relies on. Instead, `productionRequired` enters every live-learner model that reads a Production-tier outcome as a pre-specified covariate (Analysis Plan, Covariates, below): H3's self-explanation-quality contrast together with its own new provenance-flags-rate outcome, and H5's reviewed-Production subset. A post hoc difference in the provenance guard's own flag rate between required and opt-in classes, if one appears, is reported as a pre-specified sensitivity check under Exploratory analyses rather than folded into H3's own confirmatory contrast.

### Sample size

**The compiled effect-size and power-analysis sources already in this repo.** `_intake/research-os-k12/RESEARCH-OS-K12-SYSTEM-REVIEW.md` section 9 and `04-compliance-distribution.md` section 10 both state, as a "back-of-envelope heuristic," that a three-arm between-subjects design targeting a medium effect (Cohen's d roughly 0.4 to 0.5) at standard power (0.80) and alpha (0.05) needs on the order of 60 to 70 students per arm, and that a smaller expected effect (d roughly 0.3) roughly doubles that; both sources explicitly ask for a commissioned formal power analysis once effect size and clustering are set, rather than treating this heuristic as final. Bastani and colleagues (2025)'s guardrailed-versus-unrestricted contrast (a 127 percent practice-performance gain with no significant exam-performance drop, against a 48 percent practice gain paired with a 17 percent exam drop for the unrestricted condition) is directional support that a scoped-tool effect on unassisted post-test performance could exceed a medium effect size, but the paper reports percentage changes on a different task, population, and country than this pilot, with no standard deviation reported in the intake card this draft can convert into a comparable Cohen's d; it stands in for direction, never for a formal power analysis.

**The three meta-analytic anchors.** `PLAN-REVISION-3.md` section 2e names three published meta-analyses as anchors for the d = 0.4 to 0.5 planning assumption above; this revision states what each anchor licenses once its own population and moderator are checked against this design, past the shared observation that all three point in the same direction.

| Anchor | Pooled effect | Population and pedagogy | Moderator that bears on this design |
|---|---|---|---|
| Furtak and colleagues (2012), `furtak-et-al-2012-inquiry-based-science-meta-analysis.md` | Mean d = 0.50, pooled across 37 experimental and quasi-experimental studies of inquiry-based science teaching, 1996 to 2006 | K-12 and undergraduate science students, inquiry-based teaching versus comparison instruction | Teacher-led inquiry activities ran about 0.40 higher than student-led ones; the paper reports the gap alone, leaving each subgroup's own mean unstated, so a student-led-only figure cannot be read off directly. The constrained-AI arm substitutes Locate, Quote, and Check for part of a teacher's guidance role while a human teacher still reviews Production, a middle case between the two subgroups `PLAN-REVISION-3.md` section 2a leaves open (STRONG LEAN, unsettled) |
| Lazonder and Harmsen (2016), `lazonder-harmsen-2016-inquiry-based-learning-meta-analysis.md` | d = 0.66 (learning activities), 0.71 (performance success), 0.50 (learning outcomes), pooled across 72 studies | Inquiry-based learning across age groups, guidance present versus guidance absent within inquiry teaching | Tests a guidance-present-versus-absent contrast within inquiry learning, a different comparison than H1's own arm contrast. Guidance type moderated the effect on performance success, evidence that the workspace's own "no tool writes the answer" constraint is not a full guidance-type specification on its own (`PLAN-REVISION-3.md` section 2a). Bears more on the guidance factor above than on H1's own primary contrast |
| Chen and Yang (2019), `chen-yang-2019-pbl-meta-analysis.md` | Positive, moderated by implementation features; this corpus's own intake card reports no specific pooled Hedges' g or d value, direction and the existence of significant moderators only | Project-based-learning-versus-comparison achievement studies | The population and pedagogy closest to Research OS's own Production-tier work, with no number in this corpus to sharpen the planning figure past confirming its direction |

**The planning effect size this draft chooses.** d = 0.4, unchanged from the prior draft's own main assumption. The reasoning for keeping it, rather than moving it toward Furtak's own 0.50 pooled mean or Lazonder and Harmsen's 0.50-to-0.71 range, was not evaluated before this pass: the one anchor testing a construct close to H1's own arm contrast, Furtak's teacher-led-versus-student-led moderator, argues the closer-matching subgroup for a workspace whose guidance role stays unresolved between the two could sit meaningfully below the 0.50 pooled mean. An illustrative decomposition rather than a value Furtak and colleagues report directly: if the pooled mean weights the two subgroups evenly and the gap between them runs about 0.40, the subgroups sit near 0.30 and 0.70. d = 0.4 sits inside that spread, a middle point between the illustrative student-led estimate and the teacher-led one instead of an assumption drawn from whichever anchor reads highest. Lazonder and Harmsen's own 0.50-to-0.71 range tests a different construct and does not move the point estimate; Chen and Yang's own card carries no number to move it either. d = 0.5 stays the table's upper bound, now sourced to Furtak's own pooled mean; d = 0.3 stays the smaller-effect sensitivity case, now sourced to the illustrative student-led decomposition above.

**The naive, individually-randomized target this draft computes.** H1's primary contrast (constrained-AI arm versus full-chatbot arm on the near-transfer post-test) is one of two pre-specified primary pairwise comparisons under H1 (the other, constrained-AI versus no-AI control, tests H1b); a Holm-Bonferroni correction across these two comparisons sets the per-comparison alpha at 0.025 for the first-rejected test. Using the standard two-independent-means sample-size formula, n per arm = 2 × (z_alpha/2 + z_beta)^2 / d^2, at alpha = 0.025 two-tailed (z = 2.24), power = 0.80 (z = 0.84):

| Assumed d | Source of the assumption | n per arm, naive (no clustering) |
|---|---|---|
| 0.5 | Furtak and colleagues (2012)'s own pooled mean across 37 inquiry-based-science studies | 76 |
| 0.4 | This draft's chosen planning value, inside the spread the Furtak moderator check implies; this draft's main assumption | 119 |
| 0.3 | The illustrative student-led decomposition of Furtak and colleagues (2012)'s own teacher-led/student-led gap above, an estimate the paper itself does not report directly | 211 |

**The cluster-corrected target.** Because arm assignment is class-level, the naive n above understates the number of learners needed. The design effect is DEFF = 1 + (m_bar minus 1) times ICC, where m_bar is the average class size and ICC is the intraclass correlation of the outcome across classes. No card in this corpus supplies a validated ICC for a K-12 near-transfer or retention outcome measured inside a knowledge-graph workspace; none of the three meta-analytic anchors above reports one either, each pooling individual-level rather than cluster-randomized designs. The figures here use an assumed average class size of 25 and an ICC range (0.05, 0.10, 0.20) drawn from the range education cluster-trial design commonly reports for achievement outcomes, an illustrative planning assumption unsourced within this corpus, flagged for replacement by either a published multi-site ICC benchmark or the pilot's own first-semester variance decomposition before treating this number as final.

| ICC | Design effect (m_bar = 25) | n per arm at d = 0.4, cluster-corrected | Classes per arm (m_bar = 25) |
|---|---|---|---|
| 0.05 | 2.2 | 262 | 11 |
| 0.10 | 3.4 | 405 | 17 |
| 0.20 | 5.8 | 691 | 28 |

At every ICC in this range, the fully powered target sits far above what `PLAN-REVISION-1.md` section 3's own Phase 1 scope, "two classrooms, one semester" (`PLAN.md` section 8), or `04-compliance-distribution.md` section 9's district-MOU path can plausibly deliver in a first pilot. This draft therefore registers Phase 1 as a feasibility and effect-size-estimation pilot for H1's confirmatory claim; the fully powered confirmatory test of that same claim waits for Phase 2, consistent with `PLAN-REVISION-1.md` section 4's own verdict that question 1's core comparison is answerable at Phase 1 while its diversity addition needs Phase 2 for power. The table above is the pre-specified power target for the Phase 2 extension of this same registration, to be filed as an update once district-scale recruitment is contracted; Phase 1 alone is not expected to reach it.

**What Phase 1's feasible enrollment can detect.** If Phase 1 enrolls four to six classes total (two to three per arm across two arms, or a smaller three-arm slice at a single district-MOU site), at 25 students per class that is 50 to 75 students per arm before attrition. At n = 60 per arm, ICC = 0.10, DEFF = 3.4, the effective n per arm is roughly 18; solving the same formula in reverse for the minimum detectable effect at alpha = 0.025, power = 0.80 gives d approximately 1.0, an effect size only the largest studies in this corpus (Bastani and colleagues 2025's guardrail-versus-unrestricted contrast, in percentage terms) come near. Phase 1 is therefore explicitly underpowered for a definitive test of H1's directional claim on its own; it is adequately scoped, per `PLAN-REVISION-1.md` section 4, for H2, H4's offline sub-study, H5's engine-side calibration comparison, H6's within-class FSRS comparison, and H1's process-log and self-report outcomes, read at this scale as hypothesis-generating evidence. This draft states that distinction plainly rather than reporting a single pilot's underpowered p-value as if it settled H1.

**Sample size rationale.** Documented in the two tables and the paragraph above.

### Stopping rule

Data collection for a given semester's cohort stops at the semester's own natural end, the pre-scheduled eight-week retention probe date, a fixed point rather than a data-dependent threshold; no interim analysis triggers early stopping, since a three-arm educational pilot with human-subjects protections for minors does not support a sequential-testing design without its own separate IRB provision, which this draft does not request.

---

## Variables

### Manipulated variables

Arm assignment (constrained AI, full chatbot, no AI), class-level, per the Design Plan. Route condition (frontier-backward, forward-default), individual-level within class, for H2 only. Recognition-versus-ledger condition, individual-level among the Production-state subsample, for H3 only.

### Measured variables

Every outcome below states which `src/lib/research-os/EVIDENCE-SCHEMA.md` field or fields it reads, or names the schema gap this study surfaces when no field exists yet.

| Outcome | Hypothesis | Instrument | `EvidenceEvent` field(s) or other source | Judge and scoring |
|---|---|---|---|---|
| Near-transfer post-test, immediate and 8-week delayed | H1 | Sealed-pool transfer item, `INSTRUMENTS.md` retention probe schedule | `kind:"transfer_item"`, `itemId`, `learnerText`, `held`, plus the paired `kind:"teacher_review"` event | Automated grade (`gradeExplanation`) cross-scored by a human rater on a sampled subset, per the inter-rater procedure below |
| Independent problem-solving process measures | H1 | Native workspace logs, no separate instrument | `sessionId` groups every tool call in one sitting; time-to-first-hint, unprompted backtracking, and unaided completion rate are derived from timestamp and event-kind sequences within a session | System-computed, no judge; spot-audited by the research team against the raw event log for a sampled subset |
| Class-level production diversity (secondary, exploratory) | H1 | Pairwise similarity across a class's accepted productions on one routed target | Not an evidence-event field; reads `graph.productions.claims` text for `status = "accepted"` rows, per `LEARNER-STATE-MODEL.md` section 1's submission-versus-acceptance distinction | Automated text-similarity scoring; `production-guard.ts`'s `jaccardOverlap` (normalized token overlap, branch `feat/ros-production-guard`, PR #73, open) is now a candidate method, since it already runs the same comparison for duplicate detection below, though it has not yet been checked against Doshi and Hauser (2024)'s own similarity measure for fit before this draft treats it as final |
| Calibration: stated confidence against source-prediction correctness (secondary, exploratory) | H1 | The cognitive-forcing commit-before-reveal record on Check | `learnerConfidence`, `sourcePrediction`, `predictionCorrect`, `forcingEnabled` on the `kind:"check"` event, per `EVIDENCE-SCHEMA.md`'s "Cognitive forcing on Check" addendum | System-computed (`calibration.ts`'s `computeCalibrationSummary`, mean confidence against mean prediction-correctness per learner); no judge, since `predictionCorrect` is computed in code against the node's own citation label per `forcing.ts`'s existing contract |
| Time-to-Understanding on a routed target | H2 | Route API logs, no separate instrument | `fromStage`/`toStage` pair on the `kind:"check"` event that first raises `stage` to `"Understanding"`, timestamped against the route's own `at` field | System-computed |
| Self-explanation quality (recognition versus ledger) | H3 | The submitted production's own claim-and-warrant text | `graph.productions.claims`; no evidence-event field yet carries a per-production payment-arm label, a schema gap this study surfaces for `ros-04`/`ros-06`: a `graph.productions.payment_arm` column, named here rather than assumed to exist | Blind expert rating against the rubric `LEARNER-STATE-MODEL.md` section 4 names for Production, cross-scored by a second rater on a sampled subset |
| Production provenance-flags rate (secondary, exploratory) | H3 | The production provenance guard on submission and at accept time | Not an `EvidenceEvent` field; reads `graph.productions.source_provenance` (unverified-source rate), `duplicate_flag` (closest-match overlap at or above 0.6), and `counter_evidence_required`/`counter_evidence` (internalization-tier rebuttal requirement), per `PRODUCTION-GUARD.md`, built on branch `feat/ros-production-guard` (PR #73, open, not yet merged to `main`) | System-computed at submit time (`production-guard.ts`'s `checkSourceProvenance`, `jaccardOverlap`, `requiresCounterEvidence`); no judge, though a flagged row is what a teacher reviewer sees and acts on at `/research-os/review` |
| Check tool agreement, isolated versus full-context, and paraphrase fragility | H4 | Offline evaluation harness against the engine's held-out corpus, no live learner | Not an `EvidenceEvent` field at all; an engine-side evaluation report (a JSON or CSV output this study specifies but does not build) | Automated agreement rate against human-labeled ground truth already held by the engine's fixture corpus |
| Engine holdout calibration, with and without reviewed productions | H5 | `hte.corpus.production`'s holdout-calibration run, contingent on `ros-12`'s wiring | Reads `graph.teacher_reviews`' `decision` field to select signed-off productions, and the engine's own `hypothesize_result` fields (`posterior_before`, `posterior_after`, `tier_assigned`, `detectability`, per `tools/hypothesis-engine/docs/RESEARCH-OS-INTEGRATION.md`), not yet a real write path per `PLAN-REVISION-1.md` section 1's PR #14 note | Automated Brier score and calibration-curve computation against later-confirmed ground truth |
| Eight-week retention, FSRS-scheduled versus fixed-interval | H6 | `INSTRUMENTS.md` retention probe schedule, node-level scheduling condition assigned at enrollment | No field yet distinguishes which scheduling condition a given `transfer_item` event belongs to; this study surfaces the same kind of gap `src/lib/research-os/EVIDENCE-SCHEMA.md` already documents and proposes two new optional fields, `retentionInterval` ("immediate" \| "1wk" \| "8wk") and `schedulingCondition` ("fsrs" \| "fixed"), for `ros-04`/`ros-06` to add rather than assuming they exist | Automated pass and fail grade via `gradeExplanation`, same grading path as the Understanding-state Check |
| Understanding state | Covariate and outcome across H1, H2, H6 | The shipped Check tool | `kind:"check"`, `result`, `confidence`, `abstained`, per `EVIDENCE-SCHEMA.md`'s existing contract | LLM-judge call (`gradeExplanation`), cross-scored by a human rater against the same rubric on a sampled subset (see below) |
| Internalization state | Covariate and outcome across H1, H2, H6 | Sealed-pool transfer item plus teacher approval | `kind:"transfer_item"` (held, unconsumed until answered) followed by `kind:"teacher_review"`, `decision`, per `EVIDENCE-SCHEMA.md` | Automated grade plus a required teacher approve-or-return decision; agreement between the two is itself an outcome (`RESEARCH-QUESTIONS.md` Q3, the misclassification-rate question) |

**A lateral-reading outcome, considered and deferred.** Wineburg and McGrew (2019) and this batch's own design response (`PLAN-REVISION-3.md` section 2c, a Locate `secondSource` mode surfacing an independent node before a learner submits an explanation) motivate a `usedSecondSource` outcome reading whether a learner opened that second source before Check. The field does not exist in shipped code; `PLAN-REVISION-3.md` section 2c names it directly as a code change, not yet built. This revision registers no such outcome here for the same reason every other outcome above names its own field: there is no `EvidenceEvent` field or `graph` column to read yet. A further preregistration revision adds this outcome once `secondSource` mode ships, before Phase 1 data collection begins, following Wineburg and McGrew's own expert-novice contrast and Breakstone and colleagues (2021)'s national-scale companion finding as its grounding.

**How Understanding is scored.** `LEARNER-STATE-MODEL.md` section 1 and `EVIDENCE-SCHEMA.md` together define it: the learner submits a free-text explanation; `gradeExplanation` grounds a verdict of support, contradiction, or unknown against the node's own summary and its prerequisites' summaries only; `stage` moves to Understanding only when the result is support, confidence is not low, and the model did not abstain. This study adds a human cross-score on a sampled subset (the same sampling rule as the inter-rater procedure below) using a rubric scored on three dimensions: whether the explanation states the causal or logical mechanism rather than restating the claim, whether it uses at least one prerequisite concept the node's own summary names, and whether a rater with no access to the model's verdict would independently judge the explanation grounded. A human-model disagreement rate above a threshold set once pilot data exists (not fixed in this draft) flags the automated grader for review before it is trusted as this study's sole Understanding judge.

**How Internalization is scored.** A pass requires both an automated pass on a sealed-pool, multi-hop transfer item (`LEARNER-STATE-MODEL.md` section 4's transfer-task construction rule; see `TRANSFER-TASK-BANK.md` for the sky-blue path's own item bank) and a teacher's `"approved"` decision on that same attempt; a `"returned"` decision requires a one-line reason and leaves the learner at Understanding, per `LEARNER-STATE-MODEL.md` section 1. This study treats a learner's *pass* status on a delayed transfer item, independent of whether a teacher has yet reviewed it, as the near-transfer and retention outcome variables in the table above, and treats the teacher's separate approve-or-return decision on the same item as the reliability check against the automated grade, following `LEARNER-STATE-MODEL.md` section 4's own framing of that agreement as RQ3's misclassification-rate question rather than as a second, independent outcome.

### Indices

None beyond the fused Understanding-and-Internalization pass rate already defined above; this study does not build a composite index across the six primary hypotheses, since each targets a distinct construct and a combined index would obscure which specific hypothesis a null or positive result belongs to.

---

## Analysis Plan

### Statistical models

Every confirmatory hypothesis that involves live learners (H1, H2, H3, H6) is tested with a linear or generalized linear mixed-effects model with a random intercept for class, matching the unit of randomization; where a district contributes more than one school, a random intercept for school nests above class. The general form: `outcome ~ arm + covariates + (1 | class)`, or `outcome ~ arm + covariates + (1 | school / class)` when more than one school participates. Binary pass-fail outcomes (Internalization pass, retention-probe pass) use a mixed-effects logistic model; continuous outcomes (near-transfer score, time-to-Understanding, self-explanation rubric score) use a mixed-effects linear model. H4 and H5, which run against the engine's own held-out or fixture corpus rather than live learners, use the engine's existing evaluation methodology (agreement-rate comparison for H4, Brier score and calibration-curve comparison for H5) with no class random effect, since no learner clustering applies to either.

### Covariates

Pre-specified for every live-learner model: the diagnostic-probe baseline score (`src/lib/research-os/probe.ts`'s cold-start probe, where administered), grade band, a binary Title I-versus-gifted-outreach-channel indicator (per `04-compliance-distribution.md` section 12's equity plan), and a teacher-identity term nested under the class random effect where one teacher runs more than one section. Two more pre-specified covariates apply where the underlying field exists: `guidanceLevel` (the adaptive `high`/`medium`/`low` field on the `check` event, per Guidance and forcing factors above, once branch `feat/ros-faded-guidance` merges) enters H1's and H2's models; `productionRequired` (the per-class required-versus-opt-in field, per Required participation and misconduct risk above) enters every model that reads a Production-tier outcome, H3's self-explanation-quality contrast together with its own new provenance-flags-rate outcome, and H5's reviewed-Production subset. `forcingEnabled` is logged on every `check` event but not entered as a covariate in Phase 1, since the switch is fixed on for every class and carries no variance to test. Prior Academy mastery-profile proficiency (`fusedConceptMastery`, `src/lib/academy/mastery.ts`), where the learner has an existing Academy account, is added as an exploratory covariate rather than a pre-specified one, since `LEARNER-STATE-MODEL.md` section 3 confirms no code path today joins Academy proficiency to a Research OS row, so this covariate reaches only the subset of learners a manual join can recover.

### Inference criteria

Nominal alpha = 0.05, two-tailed, per confirmatory hypothesis family (H1 through H6), each treated as an independent pre-registered primary question per the Sampling Plan's own reasoning. Within a hypothesis family that carries more than one pairwise contrast, Holm-Bonferroni correction applies across that family's own contrasts before any result within it is called significant; H1's two contrasts (constrained-versus-chatbot, constrained-versus-control) are the concrete case, corrected as shown in the Sampling Plan. Power at 0.80 is the pre-specified target for the Phase 2, fully powered extension of this registration; a Phase 1 result that fails to reach significance at the underpowered enrollment the Sampling Plan describes is reported as inconclusive on H1's own terms alone, never treated as evidence against it, and the hypothesis-generating outcomes (H2, H4, H5, H6, and H1's process-log and diversity measures) are reported descriptively regardless of H1's own confirmatory result.

### Data exclusion

Excluded before the confirmatory analysis runs, following standard cluster-RCT convention: a learner who withdraws consent at any point (data collected before withdrawal is deleted outright from the analysis dataset, per the consent language in `IRB-PACKET-OUTLINE.md`); a learner who enrolls after class-level randomization has already run (logged as an observed non-randomized case, per the Sampling Plan); a class with fewer than five enrolled and consented learners in any arm, an integrity floor below which class-level clustering assumptions do not hold; and a learner whose logged workspace engagement falls below a minimum-use threshold set once pilot baseline engagement data exists, following Oreopoulos and Low (2026)'s finding, cited in `PLAN.md` section 3, that a well-funded tutoring intervention produced near-zero gains among students who did not open the tool, the same engagement-rate risk `LEARNER-STATE-MODEL.md` section 4 names as the Access-state outcome variable. The primary analysis is intent-to-treat (every consented, randomized learner analyzed in their assigned arm regardless of engagement level); a per-protocol analysis restricted to learners above the engagement threshold is reported as a secondary, pre-specified sensitivity check alongside the intent-to-treat result. A class's own Production-requirement status, `required` or `opt-in`, is not a data-exclusion criterion; see Required participation and misconduct risk above for how it enters the model instead.

### Missing data

A learner with a missing immediate or delayed post-test, but at least one other recorded outcome, is retained in the mixed-effects model, which handles unbalanced data by design; a learner with no recorded post-test at any interval is excluded from that specific outcome's analysis and reported in an attrition table by arm. No imputation is planned for the primary confirmatory analyses; a missing-completely-at-random assumption is tested by comparing attrition rates and baseline-covariate distributions across arms before results are interpreted, and any arm-differential attrition is reported alongside the primary result rather than adjusted away.

### Exploratory analyses

Named explicitly here so they are not later reported as if pre-specified: the class-level production-diversity secondary outcome under H1; the calibration secondary outcome under H1; the Production-tier scientific-understanding probing pass named for overlap-map question 12 in the Study Information section above; the production provenance-flags-rate secondary outcome under H3; the prior-Academy-proficiency covariate; the per-protocol sensitivity analysis under Data exclusion; the required-participation sensitivity check comparing the provenance guard's own flag rate between required and opt-in classes; and any subgroup analysis by grade band, gifted-versus-Title I channel, or route condition not already named as a primary contrast.

---

## Other

**Registration timing.** This draft is written and dated before Phase 1 data collection begins, consistent with the definition of a preregistration; it will be filed on OSF, and the OSF registration timestamp will be the study's registration date of record, once the IRB approval and partner-site confirmation named in `IRB-PACKET-OUTLINE.md` exist, since OSF's own form additionally asks whether an IRB or ethics board has approved the study before registration, a question this draft cannot yet answer affirmatively.

**Relationship to the founder's PhD application.** As `04-compliance-distribution.md` section 10 and `RESEARCH-OS-K12-SYSTEM-REVIEW.md` section 9 both state, the founder is simultaneously the product's builder, the study's designer, and the person whose PhD application the result serves, a named conflict of interest whose mitigation lives in the partner-PI structure in `IRB-PACKET-OUTLINE.md`, this preregistration on its own settling none of it.

## Data availability statement

Operational interaction data (which node a learner opened, which explanation they submitted, which teacher decision followed) is generated and stored under the district's data privacy agreement, governed by FERPA's school-official exception per `04-compliance-distribution.md` section 1, and is never redisclosed outside that operational relationship without separate consent. Research use of that same interaction data runs under a legally distinct basis: FERPA's studies exception (34 CFR 99.31(a)(6)) is a narrow, school-initiated pathway that `04-compliance-distribution.md` section 10 and `RESEARCH-OS-K12-SYSTEM-REVIEW.md` section 9 both state does not cleanly cover a vendor's own dissertation research, so the research consent track named in `IRB-PACKET-OUTLINE.md`, and never the operational school-official exception, is the actual legal basis for any research use, analysis, or publication of this data. Raw learner-authored text (explanations, transfer-item answers, production drafts) stays access-restricted to the named research team under the data governance model `04-compliance-distribution.md` section 10 describes, an independent data-access reviewer, ideally the partner faculty PI, holding a technical or procedural check on what the founder can access from live accounts outside the consented research cohort; this text is never posted publicly. A de-identified derived dataset, aggregate outcome variables and model-fit statistics only, with any cell too small to protect a single learner's identity suppressed, may be deposited as an OSF component after publication, gated on the IRB-approved data-use agreement and the partner PI's sign-off, and on a retention and destruction schedule tied to the study's own end date rather than kept indefinitely, per `04-compliance-distribution.md` section 10's own instruction to keep the research data-use agreement legally separate from the operational school DPA.
